# CrewLogicAI — Hub

_Migrated from `docs/STATUS.md` on 2026-06-11. This file is the **single source of truth for
project / build / production-rollout status** and the active task/backlog surface._
Read it *first* for any status question — do not re-derive status by re-analyzing `index.html` or the
edge functions unless an entry is stale (see freshness rule) or you are doing a scheduled re-verify.

## Update protocol (check → update → recheck)

1. **Check (before a change):** read the relevant row here to confirm current state before starting work.
2. **Update (during the change):** in the *same commit* as the change, update the row — Build / Prod /
   Version / Open items — so the tracker never lags the code.
3. **Recheck (after the change):** verify the change landed as described and stamp **Last verified** with
   today's date. If you couldn't verify a field (e.g. prod data), say so in Notes rather than guessing.

**BOTH surfaces, same commit (binding).** This Hub carries status in **two** places: the `SHIPPED (vX.Y.Z…)`
prose blocks below, and the **Future-Work Register table** above. A ship MUST update **both in the same
commit** — write the prose block *and* flip the matching **FW-NN row** to `Done` / `Partial` (with a code
anchor: `file:line`, symbol, or commit SHA). Updating only the prose is the drift mode that actually
happened: on 2026-07-18 the register still read `Open` for FW-32 (billing, live since v5.50.58), FW-01/FW-02
(Linxup, shipped), and FW-34 (trial lockout, shipped) — every one of them had an accurate prose block three
screens down. If a ship has no FW row, either add one (next free ID) or write `no FW row` in the commit
body so the omission is deliberate rather than forgotten.

**Status claims cite code, not this file.** When answering "is X done?", verify against the **code** — grep
the flag/symbol/table, or find the merge commit. Do **not** answer from a register row or a prose block
alone; those are what drift. Same discipline applies to scope: before calling something broken, trace which
function actually backs the surface in question — on 2026-07-18 FW-39 was flagged a live #56 bug because two
Eastern-hardcoded functions were found without checking that a *third* (`crewlogic-dispatch`) already
resolved the franchise timezone for the boards in question.

**Freshness rule:** trust an entry whose *Last verified* is recent. If it's old or the code in that area
changed since, re-verify before relying on it. Never overstate prod rollout — "code deployed" ≠ "in active
use by a prod tenant"; keep those distinct.

**Status legend:** `Done` · `In progress` · `Planned` · `Not started` · `Dropped`
**Prod rollout:** `Live` (in prod & in use) · `Deployed` (code/fn in prod, not yet exercised by a prod tenant) · `Dev only` · `n/a`

## Future-Work Register (CANONICAL — stable IDs, do NOT renumber)

**Numbering rule (binding):** FW-IDs are **permanent**. A completed item **keeps its ID** marked `Done` (never reused, never renumbered); new work gets the **next free FW-NN** (currently FW-52+). Never re-sort or re-number existing rows. This register and **`docs/CrewLogic-Future-Work-Register.xlsx`** (full detail: what / next step / evidence / priority) share these exact IDs. Status here is the **canonical** item status — it supersedes any older prose callout below (those remain as shipped-history/audit trail per the Hub guardrail). Statuses verified against code 2026-07-05 (`Open`=not started, `Partial`=core shipped + named remainder, `Done`=fully shipped).

| ID | Status | Item |
|----|--------|------|
| FW-01 | Done | Linxup live end-to-end validation — confirmed working on #56; temp debug capture stripped (`b5019cd`, merged `f408400`). Re-verified 2026-07-18 |
| FW-02 | Done | Linxup job-level arrive/leave matching — Linxup geofence create/delete live (`crewlogic-geofence-create/index.ts:108-173`), per-job sync w/ `wo_id`+`job_id` (`crewlogic-job-geofence-sync/index.ts:196`), migrations 0043/0046. Re-verified 2026-07-18 |
| FW-03 | Done | #90 Motive already live in prod — pull token connected (3 trucks) + webhook secret configured; migration 0033 preserved it, re-entry never needed (verified 2026-07-05) |
| FW-04 | Open | Refine dashcam/fault-code event labels |
| FW-05 | Open | Optional "N trucks on site" count |
| FW-06 | Open | Telematics partnership / referral links |
| FW-07 | Open | Estimates Desk iCal calendar lane (Chunk 4) |
| FW-08 | Open | Per-appointment Vonigo cancel (Vonigo ticket pending) |
| FW-09 | Partial | Real route optimization — geocoded server tools exist; recombine engine remains |
| FW-10 | Dropped | In-app reschedule ("pull it forward") — KILLED 2026-07-05 (drag-move + voice move cover it) |
| FW-11 | Open | Route Optimizer Round-2 ideas |
| FW-12 | Open | Route Optimizer multi-tenant re-architecture (off n8n + owner Sheet) |
| FW-13 | Open | Per-facility wait times |
| FW-14 | Dropped | Weather alerts narrow-scope — KILLED 2026-07-05 (state-level on dashboard + Job Plan is enough) |
| FW-15 | Done | Voice command: move job between routes |
| FW-16 | Done | Voice Dispatcher — prod write path (move/cancel/duration) verified working (owner 2026-07-05) |
| FW-17 | Done | Town/city title-case — job alerts + schedule board (chip + popup); prod v5.50.2 (2026-07-05) |
| FW-18 | Open | Vonigo date-range CRM pipeline |
| FW-19 | Open | National Accounts auto-fill job Summary (nightly cron) |
| FW-20 | Partial | Vonigo submit server-side null-price guard (frontend guard live; server guard deferred) |
| FW-21 | Done | Deploy native CPL fix to prod |
| FW-22 | Done | Shed estimator undercount fixed (walls+floor+pitched-roof + framing/shingle inputs + breakdown) — prod v5.50.2 (2026-07-05); factors calibratable |
| FW-23 | Dropped | Route-Optimizer/Storage 16 cy quick-buttons — KILLED 2026-07-18. 4/8/12/**16 cy** buttons already exist in both estimator variants (index.html:3645, :3795); Owner has no recollection of a remaining surface. Stale row, no known work left |
| FW-24 | Dropped | Onboarding serviceStates — KILLED 2026-07-05 (native users already enter ZIPs in pricing; redundant) |
| FW-25 | Open | New-estimate onboarding prompt |
| FW-26 | Partial | Native price-book onboarding — "no price book" notice done; home tile remains |
| FW-27 | Open | Phase 2 onboarding wizard |
| FW-28 | Open | Streamline the Settings environment |
| FW-29 | Done | Hide unused home cards — shipped as Epic C per-user tile toggles (migration 0037; `tileToggles` index.html:6035, `_applyTileMap` index.html:7804). Owner confirmed 2026-07-18 that self-serve hiding was never the intent, so the owner-assigned toggles close this |
| FW-30 | Open | index.html size / architecture review |
| FW-31 | Done | Payments + pricing model (BUILD shipped dormant; activation = FW-32) |
| FW-32 | Done | Billing activation — Epic E GO-LIVE (`BILLING_ENABLED = true`, index.html:4222; merged to main `7b23804`, v5.50.58). Re-verified against code 2026-07-18 (register row had gone stale at `Open`) |
| FW-33 | Done | Subscription field-model finishers (superseded by gate-logic + null-tier fix) |
| FW-34 | Done | Trial-expiry UX — the deferred lockout decision shipped as soft-grace-then-hard paywall (`ENFORCE_TRIAL = true` index.html:4215; `_trialExpired → hasAccess = false` index.html:7883; `a62e8d8`). Re-verified 2026-07-18 |
| FW-35 | Open | Truly-dead session "Reconnecting…" banner |
| FW-36 | Open | Harden crewlogic-trucks caller verification |
| FW-37 | Done | "Name your workspace" mis-route check (guarded) |
| FW-38 | Done | Native toolbar-fix prod promote (on main v5.50.0) |
| FW-39 | Done (dev) — **pending prod deploy** | Generalize Eastern-hardcoded date logic. **Built 2026-07-18.** New **`supabase/functions/_shared/tz.ts`** is the single source of truth (`STATE_TZ`, `resolveTimezone`, `resolveTimezoneLogged`, `todayPartsInTz`, `dayIDInTz`, `franchiseDayEpoch`); all four consumers route through it — `crewlogic-todays-workorders` (`getEasternMidnightEpoch` removed → `franchiseDayEpoch`), `crewlogic-job-plan` (`getEasternDate` → `getFranchiseDate`), and `crewlogic-dispatch` + `crewlogic-route-disposal` (local `STATE_TZ` copies → imports). The naive clock-face encoding Vonigo's date fields require is **preserved**; only the day selection changed. Quantified: at 21:30 PDT the old code resolved 2026-07-19 vs the franchise's actual 2026-07-18 — 86400s off, affecting truck-map pins + Job Plan for #56 between 9 PM and midnight local. `resolveTimezoneLogged` now **warns** on the Eastern-default fallback, so the silence that hid #56 is gone. **Deployed to dev only**; API-smoked (todays-workorders 4 real jobs; dispatch boardGrid 8 routes + 360/1200; job-plan date 2026-07-18). **Prod deploy of all 4 fns still required.** Note the frontend `CL_STATE_TZ` (index.html) is a necessary 4th copy — a browser cannot import a Deno module — keep it in sync |
| FW-40 | Partial | Onboarding: capture location + confirm timezone. **Timezone capture BUILT on dev (v5.50.90/.91), owner-tested 2026-07-18, pending promote** — Time zone `<select>` in Settings→company info (index.html ~1843 markup, `CL_STATE_TZ`/`_tzSuggestFromState`/`_tzBuildOptions` ~13477), shows the zone's current local time, suggests from state when unset. Persists via the generic `costSettings` merge — no edge fn, no migration. Two guards: an in-session pick, and a stored zone that **diverges** from its state's map value (`_tzDeliberate`) — the latter protects #54 El Paso (`America/Denver` vs map[TX]=`America/Chicago`). **v5.50.91 fixed** a .90 bug where `_tzUserChosen = !!storedTz` treated "a value exists" as "owner chose it", which — after the 0050/0051 backfill wrote a zone onto every franchise — suppressed the suggestion for all of them. **Remains open:** the location-capture half, and folding this into the Phase-2 onboarding wizard (FW-27) |
| FW-41 | Done | Reconnect crewlogic-marketing Pages Git — Owner confirmed 2026-07-18 the marketing site is publishing fine; marketing now lives in-repo (`marketing/privacy.html`, `marketing/terms.html`, `82b97a9`). The 2026-07-04 Cloudflare Git-disconnect is resolved |
| FW-42 | Open | Retire the last n8n dependency (route engine) |
| FW-43 | Partial | Estimate-charges normalization — dual-write done; reads + prod-apply + retire-blob remain |
| FW-44 | Done | Storage headroom / `STORAGE_INCLUDED_GB` — **set to 100 on prod 2026-07-18**. Owner confirmed the prod Supabase plan is **PRO** (100 GB file storage, 8 GB DB disk). The secret was previously absent, so `crewlogic-admin/index.ts:431` fell back to `\|\| 1` and the super-admin storage gauge divided by 1 GB (reading ~100× high; display-only, nothing was ever capped). Verified landed: `secrets list` shows `STORAGE_INCLUDED_GB` with digest `ad573668…`, which is `sha256("100")` — value confirmed without exposing it. Note the two quotas are distinct: **file storage 100 GB** (the `estimate-photos` bucket, what this gauge measures) vs **DB disk 8 GB** (Postgres rows) |
| FW-45 | Open | estimate-photos per-franchise path scoping |
| FW-46 | Done | Keys export redacted to MY_* placeholders + backup deleted; verified not in repo/index; file never synced/shared → keys never left the machine, no rotation needed (owner 2026-07-05) |
| FW-47 | Open | Google Cloud credentials cleanup + Street View proxy |
| FW-48 | Open | Track Photo-Analyzer volume-tier adjustments (analytics) |
| FW-49 | Open | Deferred usage-metering events (Street View / price.lookup / estimate.created) |
| FW-50 | Open | Live Vonigo-connect prod validation (2 @junkluggers.com cells) |
| FW-51 | Done | Confirm-email deliverability — inbox delivery tested working (owner 2026-07-05; DMARC/SPF/DKIM) |
| FW-52 | Done | Franchise timezone backfill — migration **0050** applied to dev + **prod 2026-07-18**. Fills `cost_settings.officeTimezone` from `officeState` where blank, so the zone is DATA rather than a silent derivation. Filled: #90 MA→`America/New_York`, #56 CA→`America/Los_Angeles`, #102 MI→`America/Detroit`, #116 OH→`America/New_York`, #36 VA→`America/New_York`. **Never overwrites** — #54 El Paso TX kept `America/Denver` (the TX state map would wrongly give `America/Chicago`) and #109 kept `America/Los_Angeles`; both verified intact post-apply. **#28 (CT) / #31 (NY) deliberately skipped** — no officeState/City/Zip at all, nothing to derive from; left blank so the missing address data stays visible (see FW-53) |
| FW-53 | Open (narrowed — tz resolved, **address still missing**) | #28 (CT - New Haven County) and #31 (NY - Nassau County) have **no office address** — `officeState`, `officeCity`, `officeZip` all null, `subscription_status` null (both look dormant). Surfaced by the FW-52 backfill. **Timezone resolved 2026-07-18** via migration **0051** (Owner: set to the same zone as #90) → both now explicitly `America/New_York`, so neither depends on the silent Eastern fallback; CT and NY are wholly Eastern so there is no split-state ambiguity. **Still open:** the missing address itself — decide whether to populate these records or retire them. Note the address gap is what makes `officeState`-based derivation impossible for them, and it is the same blind spot that hid #56's CA mis-zoning |

_Register established 2026-07-05 from a full code audit of the Hub's identified future work. **Re-verified against code 2026-07-18** (the table had drifted — FW-32/01/02/34 were marked Open/Partial despite having shipped; see the update protocol's "BOTH surfaces, same commit" rule added the same day). Current: 19 Done / 1 Partial / 4 Dropped / 29 Open. Next free ID: **FW-54**._

> **SHIPPED (v5.50.80→.88, prod 2026-07-17): Motive Card reclass tool + QuickBooks auto-post (super-admin / #90-internal only — NOT a customer feature).** New edge fns **`crewlogic-card-transactions`** (Motive Card `/motive_card/v1/transactions`, classifies gas/disposal/supplies/maintenance/other per-merchant with per-vendor overrides in **`card_merchant_overrides`**) + **`crewlogic-quickbooks`** (QBO OAuth2 + reclass **JournalEntry** post + dry-run Preview; tokens in **`quickbooks_credentials`**). Migrations **0047/0048/0049** applied to prod (verified). **Cash-basis reclass**: Motive posts to QB as **weekly ACH statements** ~2-3 days after each statement's last swipe, so the tool reclasses by **payment month** (group settled swipes by `invoice_number`→statement, pay date = last-swipe +3d, include statements paid in the selected month), NOT by swipe date. Two key fixes found reconciling to #90's books: (a) **filter to `transaction_status='posted'`** — declined **$999 pre-auth holds** were inflating totals and making them jump load-to-load; (b) QBO account lookup needs **`maxresults 1000`** (default page 100; #90 COA = 197 accts so 5101-00 etc. came back "not found"). Rebate is knowable (API field) but irrelevant to reclass — rebates only touch gas; disposal/supplies/maintenance amounts are exact. Validated May: gas $3,761.52 + disposal $2,454.90, ties to QB Motive payments ($6,227.43) within $11.01 rebate-timing. **GL map (Luggers NE COA):** gas 5101-00, disposal 5051-00, supplies 5151-00, maintenance 5102-00, other 8100-00. **QBO prod:** production keys, `QBO_ENV=production`, prod `QBO_REDIRECT_URI` (prod fn URL) set; Intuit prod redirect registered; owner reconnected on prod. **Security:** both fns deployed `--no-verify-jwt` but now **super-admin JWT-guarded** — frontend `_crEdge` sends the owner's Supabase JWT, fn resolves via `/auth/v1/user` and requires `charles.dennis@junkluggers.com` (OAuth-callback GET exempt); unauth calls get **403** (verified prod). Panel has vendor **drill-down** (date/amount/invoice #/type). **Open follow-up:** generalize the reclass beyond #90 if other franchises get Motive cards; the `listAccounts` diagnostic action in `crewlogic-quickbooks` can be pruned later. Last verified 2026-07-17.

> **SHIPPED (v5.50.32→.41, prod 2026-07-08→10): Stripe billing (wired, DORMANT) + Linxup geofence pipeline + Alerts Report + #56 timezone fix.** **(1) Stripe billing** (v5.50.32/.33) — fully wired + validated on prod but held **dormant behind `BILLING_ENABLED = IS_DEV_ENV`**: `crewlogic-billing` (migration 0025 columns applied, live price IDs Starter `price_1Tqtlc…`/Pro `…tle`/Ent `…tlf`, `STRIPE_SECRET_KEY`+`STRIPE_WEBHOOK_SECRET` set, webhook registered, redeployed **`--no-verify-jwt`** so Stripe's webhook reaches the handler). Settings→Billing **"Choose a plan"** subscribe modal + **double-subscription guard** (server 409 `already_subscribed` when a live `stripe_subscription_id` exists → routes to Portal). Stripe Customer Portal set: upgrades prorate-now, downgrades at period-end. **GO-LIVE when the Stripe account review clears:** flip `BILLING_ENABLED`+decide `ENFORCE_TRIAL`, stamp the testers' `trial_ends_at`, promote, test one real sub. Full state in [[payments-processor-and-seats-decision]]. **(2) Linxup geofence pipeline**: `crewlogic-linxup-webhook` now reads `geofence.fenceGroup`→`geofence_alerts.category` (classifies Linxup Groups like Motive categories), accepts the token **with or without** the `Bearer ` scheme, and stores on-site **`duration`** (Linxup `durationMinutes`×60 sec; Motive already stored `duration`) — all 74 prod exits backfilled. **Franchise config (via temp `linxup_webhook_debug` capture — DROP after):** #102(MI) auth'ing+storing (needs Groups set on its geofences); **#56 = Koby Agor / Orange County (NOT Mark)** is sending a **URL** in Linxup's Bearer Token field — must be CrewLogic's generated token **`geoGclkE_macF0_rCfuD5TawmwoLGyUON5mR1Bz89gw`**; #36(VA) wrong token. **(3) Geofence setup guide** = in-app **modal** (Settings→Trucks; a hosted page is shadowed by the Pages SPA catch-all `/* /index.html 200`, so modal not page): Motive Category = Linxup Group naming (**Transfer or Disposal** both →🗑️), + **Headquarters**(home base) / **Region**(Cape Cod, RI) categories = no badge. **(4) Alerts Report** (v5.50.38→.41): 📊 button off Live Alerts → modal, **Today/This week/This month** range tabs, facilities grouped under category, `visits · total · avg` per row, **<8min drive-throughs excluded**. **(5) #56 timezone**: `officeState` was **blank** (city/zip were CA) so the resolver defaulted Eastern → set `officeState='CA'` (data fix; #56 was the only blank franchise). **Open follow-up (discussed, not built):** geofence-sync (Motive/Linxup = single source of truth, pulled into CrewLogic) + facility-visit analytics data model. Last verified 2026-07-10.

> **SHIPPED (v5.50.29→.31, prod 2026-07-07/08): owner email notifications + National-Account color + Junkluggers logo default.** Merged dev→main (`d77a17c`=.29, `61bbaef`=.30, `7a58ecf`=.31) + `crewlogic-notify`/`crewlogic-signup` to prod. **(1) Owner email notifications** (v5.50.29): new edge fn **`crewlogic-notify`** sends `{subject,text}` via **Resend** from `notifications@crewlogicai.com` → `bluecollartechai@gmail.com` (`RESEND_API_KEY` on prod; **crewlogicai.com verified** in Resend). `crewlogic-signup` fires a 🎉 on new trial signups (native + JL-prospect paths, not idempotent re-clicks); frontend `submitFeedback` fires a 💬 on feedback — both **best-effort** (a Resend hiccup never breaks provisioning/feedback). Test email verified delivered (Resend id returned). **(2) National Account color** (v5.50.30): label **9981** (Nat'l Acct — e.g. Kura Sushi USA on #56) was swept into the amber "estimate-taken" bucket; now excluded from `estTaken` and painted its own reserved **dark blue `#1f4e79`** (matches Vonigo) on the board (`mjRenderBoard`) + map pins (`loadJobMarkers`). **(3) Junkluggers logo default** (v5.50.31 — owner: backfill Luggers franchises with no logo): any franchise on the JL tenant (`946a4535…`) with no `cost_settings.logoPath` falls back to the shared JL brand logo (#90's `franchise-logos/90/logo.png`) on the home header + PDF cover — via new `_effectiveLogoPath()` used by `getBrandLogoUrl` + `getBrandLogoBase64`. Uploading a logo (sets `logoPath`) overrides; removing your own falls back. Settings preview shows the default but hides Remove (only your own upload is removable). Verified the anon storage client signs #90's path cross-franchise; native (non-JL) franchises + #54/#102 (own logos) unaffected. Last verified 2026-07-08.

> **SHIPPED (v5.50.25→.28, prod 2026-07-07): at-site truck badges + geofence-CATEGORY classification + multi-tenant dispatch/map fixes (Orange County #56 onboarding).** Merged dev→main (`2b43875`=v5.50.28, `f6c40fa`=v5.50.27) + migration 0034 + 3 edge fns to prod. **(1) At-site truck badges** (v5.50.25/.26): a truck inside a disposal/recycling/donation geofence wears its **bare emoji (🗑️/♻️/🎁)** on its map icon — driven by the telematics GEOFENCE the truck is currently in (`_truckCurrentFacility` built in `_renderGeofenceAlerts` from `geofence_alerts`), not 60 m proximity. **(2) Category classification** (v5.50.27): the badge + dwell red-flag classify off the Motive geofence **`category`** (Transfer Station/Disposal→🗑️, Recycling→♻️, Donations→🎁; Job Site/Truck Stop/Uncategorized→none), falling back to the site-name match. Migration **0034** adds a `category` column to `geofence_alerts`+`motive_geofences`; `crewlogic-motive-webhook`'s `resolveGeofenceName` now captures `g.category` (Motive `/v1/geofences`) + stamps it on each alert; `_facilityForAlert()` consumes it. Reliable regardless of naming (e.g. "Lost Brothers Pallets" category=Recycling → ♻️). Franchisee setup guide drafted: `docs/geofence-setup-guide.md`. **(3) Multi-tenant fixes** (v5.50.28) surfaced onboarding **Orange County #56**: (a) the dispatch board/calendar + truck map showed the on-site **CONTACT** (Vonigo WorkOrder **field 183**) instead of the **CLIENT** account for commercial franchises — verified on job **832131** (field 183="Sandoval, Joe" contact vs client relation="Clean City Pros"); `crewlogic-dispatch` (boardGrid/listRouteJobs) + `crewlogic-todays-workorders` now resolve the name from the **`client` relation** (fallback 183); #90 residential unaffected; owner-verified 832131→"Clean City Pros" on prod. (b) `_fitTrucksCentered` only fit the map when TELEMATICS TRUCKS existed, so a franchise with jobs-but-no-trucks (or neither) sat on the hardcoded `[41.86,-70.92]` (#90/MA) center; now it **fits jobs even without trucks** and, when empty, **centers on the franchise's own geocoded home** (`_franchiseHomeLL`, captured in `renderSiteMarkers`). Both merges preserved v5.48.11–13 PDF-cover. Last verified 2026-07-07.

> **SHIPPED (v5.50.23→.24, prod 2026-07-07): Live Alerts — real-time ticking ages + inline dwell-time indicator ("on-site too long" flag).** Merged dev→main (`39db0a8`); app.crewlogicai.com serving **v5.50.24**. Frontend-only. **(1) Ticking ages** (v5.50.23): the 30s poll only re-rendered when a NEW alert id appeared, so the relative "Xm ago" ages froze until a new event / manual refresh (owner saw "just now" stuck ~12 min); now the poll **always** re-renders (scroll preserved) so ages tick every cycle and realtime-dropped alerts backfill. **(2) Dwell indicator** (v5.50.24 — owner-requested so the ops manager can see how long a truck's been at a transfer/disposal site and redirect it if too long): each truck whose **most-recent event is an entry/arrival** shows a live **`⏱ on-site <Xm>`** badge inline (ticks every 30s). Flags **RED** over threshold — facility `geofence_entry` matched (case-insensitive exact/substring≥4) to the franchise's **disposal/recycling/donation SITES** → that type's **Wait Time** (`disposalWait/recyclingWait/donationWait`; #90 = 40/15/15) → "over 40m"; customer `job_arrive` → "past end" when `now > local-midnight + startMin + durationMin` (via a `_jobsByWo` cache populated in `loadJobMarkers` from the Vonigo `durationMin`); **amber** in the 5-min pre-threshold window. **Fail-safe:** an unmatched facility geofence (fuel/parking/depot) or a `#woID` not in today's jobs cache shows the dwell but does **NOT** flag — no false alarms. **Owner note:** the red flag requires the telematics geofence NAME to match a settings disposal/recycling/donation site name. Merge preserved the v5.48.11–13 PDF-cover content (grep-verified). Owner-tested on dev. Last verified 2026-07-07.

> **SHIPPED (v5.50.18→.22, prod 2026-07-07): Dispatch — duration arrival window + collapsed layers + reliable map fit; feedback modal fixed; Live Alerts franchise-gated.** Merged dev→main (`c4938ad` = v5.50.18–.21, `2766f67` = v5.50.22) + `crewlogic-todays-workorders` deployed to prod; app.crewlogicai.com serving **v5.50.22**. **(1) Arrival window = job start → start + Vonigo DURATION** (field 186 added to `todays-workorders` as `durationMin`; threaded to the Details card via `data-jdur` + to the measure result via `j.durationMin`), fallback 2h when a job has no duration — a wide appointment block (job 850447, 9am–5pm = 480min) reads on-time instead of late (v5.50.18, confirmed 850447 returns durationMin 480). **(2) Map layers control collapses** to a hover-to-open icon (Leaflet `collapsed:true`) (v5.50.19). **(3) Map coverage-fit ROOT-CAUSE fix**: the single fit ran whenever markers loaded — if before the flex-column layout settled it locked a too-low zoom that nothing corrected (invalidateSize fixes size, not zoom); added a one-time deferred re-fit in `dashMountMap`'s settle handler (rAF/250/700/1500ms + ResizeObserver) guarded by `_dashNeedsFit` so drags/refreshes never re-zoom (v5.50.20). **(4) Feedback modal — two prod bugs**: `openFeedbackModal` did `ctx.estimate_id.slice(-8)` on a numeric estimate ID → TypeError before the modal opened (String()-coerced, v5.50.20); modal was `z-index:200`, below Leaflet's ~1000 controls → rendered behind the map (raised to 100000, v5.50.21). **(5) Live Alerts franchise-gated** (v5.50.22): the realtime `geofence_alerts` subscription had NO server-side filter and a client-side check (`row.franchise_id !== fid`) that is bypassed when `franchiseInternalID` is falsy; now the subscription uses `filter:'franchise_id=eq.<fid>'` (server-side), both REST fetches add `&franchise_id=eq.<fid>`, and all three paths FAIL CLOSED when fid is missing. **Investigation:** prod `geofence_alerts` are ALL #90's real trucks (Truck 1/2/3; **no other franchise has a single alert** yet), and dev's are all #90-dev **SEED** data (Truck 7/12 · Yard North/Depot 1 placeholder names, all `franchise_id=4444…`) — so there was **no active leak**, but the hardening matters now that Mark (+others) are onboarding telematics (Linxup). RLS SELECT was already franchise-scoped; this closes the realtime hole. Both merges preserved the v5.48.11–13 PDF-cover content (grep-verified pre-push). Owner-tested on dev before each promote. Last verified 2026-07-07.

> **SHIPPED (v5.50.13→.17, prod 2026-07-06/07): Dispatch — measure-line dismiss, expected-arrival + 2h arrival window, map-refresh-after-move.** Frontend-only merges to `main` (v5.50.13 `8bc8429`, v5.50.17 `3710d6a`); app.crewlogicai.com serving **v5.50.17**. **Measure route line** reliably dismissable (× / click-away / after refresh) — flagged `{_measure:true}` + swept off the map (reference could be lost across a re-render) (v5.50.13). **Expected arrival time** on the truck→job arrival displays (Details card nearest-truck line + the measure-distance result): "Expected Arrival: <now + drive minutes>" (browser-local; ok while browser tz == franchise tz) (v5.50.14/.15). **2-hour arrival window** (owner logic): compare expected arrival to the job's scheduled start → start+2h — **green ✅** within (incl. early), **red ⚠️ "outside arrival window" + a contact-customer phone/text block** when later; truck→**site** (disposal/recycling/donation) or any job with no start time → plain arrival, no window (guarded on `j.time`/`j.jobID`); disposal path never gains window logic (v5.50.16). `_arrivalInfo()` supersedes `_arriveText`; job start (`j.time`, min-from-local-midnight, fallback `_dispClockToMin(timeLabel)`) threaded to the Details card via `data-jtime`/`data-jtlabel`. **Map refreshes after a job move**: the drag/voice move-success handler reloaded the boards but not the map, so a job moved to another route kept its old marker color until a manual Refresh — added `loadJobMarkers()` (clears+refetches+recolors) to the dashboard move path (v5.50.17). Also this session: restored soft-deleted estimate **1783080981668** (#90, was a draft, deleted 7/3) via a scoped prod UPDATE. Owner-tested each on dev before promote.

> **SHIPPED (v5.50.7→.12, prod 2026-07-06): Dispatch — payment-based "done", estimate-taken amber, arrival time, map-fit + measure-dismiss.** Promoted in two pushes (v5.50.8 `815e561`, v5.50.12 `cdb262b`); app.crewlogicai.com serving **v5.50.12**. **Geofence tracking now ends on PAYMENT only** — status Completed/Archived (164/165), which for #90 is set when payment is taken — and **no longer on the estimate label** (`crewlogic-job-geofence-sync`: dropped `labelDone` from create-skip + delete pass; same-day cancel handling kept). Verified live on job 861720 (2026-07-06): estimate-create flips the 245 "done" label True while status stays Open, "do work now" flips it back False, and only **payment → status 165 Archived**; the label flaps and fires pre-payment, so it's invalid as a tracking-done signal. **"Estimate taken" status color** (owner option B): any UNPAID job whose label isn't New Appt (9984)/no-label → reserved **amber `#ffab00`** (verified outside every route/zone/label palette, never painted on a route); gray reserved for PAID (164/165) or Lost (9993); New Appt/no-label → normal color. Applied to BOTH the schedule board pills (`mjRenderBoard`, uses `j.labelOpt`) AND the truck-map job pins (`loadJobMarkers`; `labelOpt` added to `crewlogic-todays-workorders` so the map can distinguish states). Est-Only (9996) stays amber (owner). **Details card**: nearest truck recomputes from CURRENT positions on every click + shows drive-time ETA AND **expected arrival clock time** (now + drive minutes, browser-local — correct for the ET franchise; revisit for cross-TZ). **Measure route line** is dismissable (× on the Details card, or click any other truck/job/site). **Map-fit-on-load fix** (v5.50.8): `_fitTrucksCentered` calls `invalidateSize` before `fitBounds` so the column layout's stale pane width no longer zooms the map out to the whole Northeast. Regression-safe: measure tool, Live Alerts, disposal ranking, job phone slot untouched. **Open follow-ups:** owner to confirm on prod that a real crew's geofence stays active until payment + estimate-taken jobs show amber; map-pin amber overrides route hue for those pins (owner-chosen, flip to route-color is 1 line).

> **SHIPPED (v5.50.6, prod 2026-07-06): Dispatch dashboard left-rail + road-route disposal + Google geocoding + same-day-cancel geofences.** Owner-tested on dev, promoted `dev`→`main` (`da9e200`) + 2 edge fns to prod; app.crewlogicai.com serving **v5.50.6** (HTTP 200 verified). **Frontend (v5.50.3–.6):** floating job/disposal cards moved into a **permanent left info rail** mirroring the right Live Alerts rail — `[left rail | map | Live Alerts]`, map **fixed** (no recenter on click/result); two **collapsible** sections (📍 Details + 🗑️ Cheapest & Fastest); **drag-resizable dividers** with widths persisted per-browser (`localStorage dashRailW_left/right`); **all marker clicks — job/truck/site — open in the rail on CLICK not hover** (measure result too); disposal route lines now follow **real TomTom road geometry** (per-leg via `_tomtomRoutePoints`), split **truck→site SOLID + site→job DOTTED**, with the **site's own emoji icon** (🗑️/♻️/🎁) instead of a dot; rail sections `flex-shrink:0` so they scroll instead of clipping. Desktop dashboard only. **Backend (`crewlogic-todays-workorders` + `crewlogic-job-geofence-sync`):** (1) **Google-first job geocoding** (rooftop) + Census fallback — coords were Census interpolation (approximate/wrong-street; e.g. 36 Fieldcrest Dr landed on Benton St), now Google via the existing `GOOGLE_GEOCODING_API_KEY`, cached per address with `provider='google'`, and **old Census rows auto-upgrade on next fetch** (no cache wipe / migration). Fixes job pins, the **<60m arrival house-on-truck icon** (never showed off bad coords), AND the 75m job-site geofences (all one coordinate). (2) **Same-day CANCEL now deactivates a job's geofence** on the next 30-min sync (was: lingered to the 2:30 AM EOD sweep → false "arrived" alerts) — `todays-workorders` gains opt-in `includeCancelled` + `isCancelled` flag (picker default unchanged); the sync deletes on explicit done OR explicit cancel (still never on "missing from list" — transient-hiccup-safe). **Regression-safe:** Live Alerts, measure tool, Motive crossings, job phone slot, disposal ranking (already road-based) untouched; prod `isCancelled` confirmed present. **Open follow-up:** owner to confirm on prod that a real crew arrival shows the house-on-truck icon + a same-day cancel drops its geofence.

> **SHIPPED (v5.50.0, prod 2026-07-05): Telematics consolidation — Motive + Linxup expanders (API + Webhook) + Linxup webhook receiver.** LIVE in prod: migration 0033 applied + verified (both prod franchises preserved/active: `209f31ef`/Linxup + #90/Motive; `linxup_webhook_config` + 4 RPCs created), `crewlogic-linxup-webhook` + `crewlogic-settings` deployed to prod (receiver 401s on no-auth), `dev`→`main` merged `d8439c3`, app.crewlogicai.com serving v5.50.0. Migration applied via `supabase db query --linked --file` (prod migration-tracking table is EMPTY → `db push` would re-run 0012–0032, unsafe). **Live Linxup end-to-end still PENDING** — prod Linxup franchise `209f31ef`'s cameras/devices are disconnected (no geofence activity); validate when back online by generating the Linxup token in their Settings→Trucks and pasting URL+token into their Linxup webhook. **Cloudflare incident (2026-07-04→05):** the `crewlogic` Pages project went **Git-disconnected** (owner account change) → dev+prod deploys silently stopped after v5.49.1; reconnecting Git restored both. `crewlogic-marketing` (crewlogicai.com) likely still needs the same reconnect. Owner-approved plan: **`docs/plan-telematics-consolidation.md`**. Reorganizes Settings→Trucks into two provider expanders (Motive, Linxup), each with API-pull + Webhook-push; builds the missing **Linxup webhook** side (new `crewlogic-linxup-webhook` — Bearer in `Authentication` header, 201 response, log-then-parse both flat `pushType` + V3 `eventType` formats → `geofence_alerts`) + storage (`linxup_webhook_config` mirroring `0027`). Owner decisions: **one ACTIVE pull provider at a time BUT store both providers' creds** (`telematics_credentials` → per-provider + `is_active`; #90 needs both Motive+Linxup for testing, so saving Linxup must NOT wipe Motive); **all-at-once** rollout (dev-first). Motive storage + live webhook receiver **untouched** (regression guard). Scope boundary: Linxup **fence-name** alerts only in Phase-1; Linxup **job-level** arrive/leave matching (needs Linxup geofence-create) = follow-on. Refs: `docs/PushAPIV3.pdf`, `docs/LinxupPushAPI.pdf`, Linxup pull swagger `api.linxup.com/pullapi`. Building backend (migration + 2 fns) on dev now; UI reorg next.

> **SHIPPED (v5.49.1, prod 2026-07-03): Vonigo submit — never send a null price item.** Fix for estimate 1782846616115 failing to submit with an opaque Vonigo **"data validation error"**. Root cause: its discount line was built by an **external PDF-recreation** (not the in-app picker), so it had **no `priceItemID`**; the submit charge builder (`executeVonigoSubmit`, ~index.html:16649) sent `String(undefined)` → Vonigo rejected the **whole quote**. Volume charges were immune (they resolve their ID from the price book); only hand-built/recreated **non-volume** charges hit this. (Confirmed the in-app description edit is NOT involved — `updateChargeFromInput` writes only `charge.description`; `name` is a separate field.) Fix adds `resolveChargeItem(c)`: a non-volume charge missing a valid `priceItemID` resolves it from the live price book by type (**discount → the "Discount" item `61664`**, the same ID a picker-added discount gets — so a recreated discount now **auto-heals and submits**); anything still unresolvable is collected and the submit **aborts BEFORE calling Vonigo** with an actionable message naming the charge, instead of the opaque rejection. Amounts (qty/unitPrice → fields 9288/9287) untouched → **no pricing change**. FRONTEND-ONLY (dev→main no-ff merge `a6196ad`). **Validation caveat:** dev is `VONIGO_READONLY` so the real submit can only be proven on prod — logic verified by tracing the actual failing charge data (null discount → resolves to 61664, not flagged). Deferred: optional server-side guard in `crewlogic-estimate`.

> **SHIPPED (v5.49.0, prod 2026-07-03): multi-room photo capture screen + EXIF orientation fix.** New stable full-screen capture surface `#photoSessionScreen` (z-index 280): the editor **📷 Photo** button (`openPhotoSession`) opens it → shoot a room, **➕ New Room** adds another and STAYS on-screen, a tappable **room strip** lets you jump back to fix any room, **💾 Save** turns each room with ≥1 photo into a new blank `type:'volume'` charge (empty rooms dropped), then the crew analyzes rows / Analyze All in the editor as before. Fixes the owner-reported "bouncing" (old floating overlay called `renderCharges()`+`scrollTo(0,0)` on every action → buttons migrated off-screen, saves missed); the new screen never mutates the editor DOM until Save. Reuses the existing IndexedDB queue + background upload pipeline (`queueUnassignedPhotoForRoom`/`_uploadPhotoFromQueue`/`idbReassignChargeIdx`) — no data-model change. Separately fixed sideways iPhone photos: `compressChargePhoto`/`compressForAnalysis` now decode via **`compressOriented()`** = `createImageBitmap({imageOrientation:'from-image'})` (EXIF baked into pixels; `<img>` fallback) — app-wide, so per-row camera benefits too. **Preserved:** per-row camera icon, Analyze/Analyze All, legacy room overlay (`startPhotoCapture`, no longer the entry point), save pipeline. FRONTEND-ONLY (dev→main no-ff merge `d7234fb`; no edge-fn/migration). Verified **app.crewlogicai.com serving v5.49.0** (HTTP 200). Device QA (camera/orientation/upload) pending owner field use. Minor open: room-strip labels "Rm 1/2…" vs plain numbers (owner preference, not blocking).

> **Prod fix — 2026-07-03: geocoder Census→Google fallback (`crewlogic-dispatch`).** Site markers (disposal/recycling/donation + route-opt jobs) geocode via the free **US Census** geocoder, which returns NO match for some formats (e.g. "805 Gifford Street Ext, East Falmouth MA" — confirmed empty; OSM/Nominatim also fails; only Google resolves it) → the marker was silently skipped. Fixed `geocode()` to fall back to **Google Geocoding** (`GOOGLE_GEOCODING_API_KEY`, same key `crewlogic-geofence-create` uses) when Census misses, and to **re-try a prior census-only cached miss** (so it auto-heals — no cache clearing; a google miss is cached final via `provider`). Deployed to **dev + prod** (backend-only, no frontend/version change). Verified: the address now returns 41.586,−70.605. NOTE: pending frontend promote still on dev = route-line + bare marker icons (v5.48.30).

> **REFERENCE — CRM integration field inventory (2026-07-03):** `docs/crm-integration-field-inventory.md` — living tracked map of every CRM (Vonigo) field/enum/operation CrewLogic uses, with empty **SFDC**/**ServiceTitan** columns to fill when evaluating a CRM migration. Built from a full audit of the 7 Vonigo edge fns + `index.html` + schema. Key: the `tenants.pricing_source/customer_source/submission_target` gates are the existing off-Vonigo seam; no abstraction yet for the numeric field-IDs, the Job-vs-WorkOrder split, the quote wire-shape, or auth. Keep current as CRM touch-points change.

> **BACKLOG / OPEN ITEMS (owner-flagged 2026-07-02):** (1) **Streamline the Settings environment** — owner: "the whole settings environment is a mess, need to streamline it… later." Bigger UX/reorg pass; not started. (2) **Town title-case** — job-alert towns render UPPERCASE from Vonigo (FALL RIVER); title-case is a quick display tweak if wanted. (3) **SHIPPED (v5.48.35, prod 2026-07-03): Traffic layer on the trucks map — now VECTOR.** Evolved raster→vector: started as TomTom raster flow tiles (v5.48.26), but raster can't road-class-filter (a zoom-offset attempt was too blurry, reverted). Final = **TomTom VECTOR flow tiles** via **Leaflet.VectorGrid** (`flow/relative/{z}/{x}/{y}.pbf`, tile layer "Traffic flow", props `road_type` 0-8 + `traffic_level` 0-1): style fn colors by congestion (green/amber/orange/red), widths by road class, and a **zoom/road-class filter** (`maxClass = zoom<9?1:zoom<11?3:zoom<13?5:8`) → **major roads only when zoomed out, all roads zoomed in, crisp at every zoom**. Single **🚦 Traffic** toggle, **default ON**, under Hybrid (`tt-traffic-first` CSS), persisted (`crewlogic_map_layers`). Key `TOMTOM_TRAFFIC_KEY` (~index.html:5452), domain-locked — ⚠ **whitelist `app.crewlogicai.com` + `dev.crewlogic.pages.dev` in TomTom** (else tiles 403). Same batch: **route/measure line green→blue `#1e6fff`** (distinct from green traffic; reads on light + satellite); **measure now draws the real TomTom driving route** (Routing API) not a straight line, with a Google/straight-line fallback; **bare disposal/recycling/donation map icons** (no ring, ~20px, home keeps its ring). Scope doc: `docs/plan-vector-traffic.md`. (Google traffic can't overlay Leaflet; app already had traffic-aware ETAs via Google Distance Matrix.) (4) **Linxup telematics webhook path** — for non-Motive franchisees; Linxup Push API uses a **Bearer token** (not Motive's HMAC-SHA1 sig) → receiver needs a 2nd auth path. Owner getting a dedicated DEV Linxup webhook via support. (5) **SHIPPED (v5.48.21, prod 2026-07-02):** per-user SMS text-prefill — mirrors the email prefill; per-user template in `profiles.sms_template` (migration 0032, applied dev+prod) via `saveSmsTemplate` (Bearer-auth'd, patches only caller's row) in `crewlogic-settings`; `buildSmsBody` fills `{{customer name}}`/`{{user name}}`/`{{company name}}`; `_smsHref` iOS `&body=` else `?body=`; wired the 3 `sms:` links (estimate bar + job phone + mjJob popup). Config in Settings→Proposal. Plus **Company Nickname (DBA)** (`cost_settings.companyNickname`, no migration — generic merge) so the text's `{{company name}}` uses the casual name (Junkluggers) while proposal/email keep the formal name. `sms:` prefill is mobile-only (desktop OS-dependent).

> **Prod update — 2026-07-02 (v5.48.18): dispatch calendar picker + town on job alerts.** Promoted `dev→main` (clean merge, no divergence). (1) **Calendar dropdown** (`<input type="date">`) added to both dispatch schedule boards (Source+Dest) via `dashApplyDay`/`dashSetDay` — jump to any date; ◀/▶/Today stay in sync (trucks-map bar already had one). (2) **Town** on `job_arrive`/`job_leave` alerts: `crewlogic-job-geofence-sync` parses town from the WO address (`townFromAddress()`) + appends to the geofence name (`"client · #woID · TOWN"`); display `_geofenceAlertRow` splits line1=🚚 client / line2=`#woID · TOWN`. **Prod backfill:** the 20h re-create guard blocks a plain sweep+recreate, so → swept the 6 off Motive + hard-cleared today's `job_geofences` rows (`delete … where created_at > now()-interval '20 hours'`) + recreated with towns (DENNIS/NORTH PROVIDENCE/NEWPORT/PLYMOUTH/FALMOUTH/FALL RIVER; McGuirk skipped=done). `crewlogic-job-geofence-sync` redeployed to prod. Open cosmetic: towns are UPPERCASE from Vonigo (title-case if owner wants). **(v5.48.19)** map measure-distance fix: the green dotted `_measureLine` now clears when the result box is dismissed (`_mapHoverHide` removes it) — before, only the cancel banner cleared it, so the line lingered with no way off. Frontend-only, promoted `dev→main`.

> **PROD CUTOVER — 2026-07-02 (v5.48.16): Motive geofence truck notifications + JOB arrive/leave NOW LIVE IN PROD.** `dev→main` merged; prod serves **v5.48.16**. Migrations **0027–0031 applied to prod** (via `supabase db query --linked` with a `"SELECT 1; $(cat file)"` wrapper to dodge the leading-`--` flag-parse + preserve `$$`; idempotent — verified 4 tables, RLS `geofence_alerts_sel`, realtime on `geofence_alerts`, `current_franchise_id`/`get_motive_webhook_secret`/`get_telematics_credential` present, #90 telematics_credential=motive). **4 fns deployed to prod** (`crewlogic-motive-webhook`, `crewlogic-settings` [+additive Motive actions], `crewlogic-geofence-create`, `crewlogic-job-geofence-sync`, `--no-verify-jwt`). **Prod crons active** (sync `*/30 * * * *`, eod-sweep `30 2 * * *`); first prod sync created **6 job geofences** (names resolving). **DEV crons disabled + 6 dev geofences swept** (single #90 Motive account → no dup). Owner re-pointed the Motive webhook to the prod receiver + saved the signing secret. ⚠ **First secret save (12:24) was the WRONG secret → receiver 401'd every crossing** (verify failed) → nothing stored despite events firing; symptom = dev stopped at 12:21 but prod stayed empty. Owner re-saved the CORRECT secret at 12:34:34. **CONFIRMED end-to-end 12:43–12:47**: real crossings verify + land in prod with names resolving (e.g. "Cape Cod - Region" 12:43, "Borne - Transfer" 12:47, Truck 3). Lesson: a wrong signing secret fails silently (401, no store) — check the secret first when events fire but nothing lands. CLI left linked to PROD. Disable prod crons if needed: `select cron.unschedule('crewlogic-job-geofence-sync')` + `...-eod-sweep`. This SUPERSEDES the branch-divergence noted below (dev is now merged to main).

> **Prod HOTFIX — 2026-07-01 (v5.48.13): PDF cover Subtotal/Discount + right-aligned total + symmetric buffer (isolated cherry-picks).** prod (`origin/main` = `c0b9746`) serves **v5.48.13** — FRONTEND-ONLY. Cover page now shows **Subtotal − Discount = Estimated Total** (only when discounted), positioned deposit-aware so it doesn't collide with the Payment Schedule block; the Estimated Total right-aligns to `lineRight` so its digits line up flush-right under the Subtotal/Discount (invoice-style — owner=accountant); **v5.48.13** insets `lineRight` to `rCol-30` (10pt) so the amounts clear the green-box right edge with a buffer matching the label's 10pt left buffer. Shipped via **cherry-pick of the cover commits** (`04193b9`, `d0c79e8`, + the buffer commit) onto main — NOT a `dev→main` merge — because **dev v5.48.7–v5.48.10 = the in-progress geofence-alerts feature (settings field, receiver, dashboard rail, mobile card + migrations 0027/0028/0029 + `crewlogic-motive-webhook`/`crewlogic-settings` changes) is NOT prod-ready** (prod receiver/webhook not wired) and deliberately stays on dev. ⚠ **Branch divergence:** prod = v5.48.6 + the cover cherry-picks, labeled v5.48.13; dev = v5.48.13 with the geofence work too. When geofence eventually ships, reconcile (dev→main will merge the geofence commits; the cover cherry-pick is already on main). Owner authorized as a hot patch (couldn't test on dev). Verify on prod: generate a proposal PDF for a discounted estimate (± deposit).

> **DEV feature (not prod) — 2026-07-02 (v5.48.16): Motive geofence truck notifications + JOB arrive/leave.** Full pipeline on `dev` only. **Realtime reliability fix (v5.48.16):** the alerts list was realtime-only and silently dropped events on a reconnect (owner missed two real storage-geofence exits 2026-07-02, though they WERE captured + correctly attributed to #90=`44444444…`). Added a **30s poll fallback** (both surfaces): refetch newest-50 + merge any unseen `id`s (shared `_shownAlertIds` dedup vs realtime); realtime kept as instant path + re-polls on reconnect. **Lifecycle notifications (v5.48.15):** the sync emits `geofence_created` ("📍 Tracking started — \<client\>", once/job) and `geofence_deleted` ("🏁 Tracking ended" on job-complete / "🌙 …(end of day)" on sweep) into `geofence_alerts`, rendered muted/smaller so 🚚 crossings stand out (best-effort inserts; owner: "pull back later if too noisy"). (a) **Geofence/telematics alerts** (Phase 1): per-franchise Motive webhook secret in Settings→Trucks (Vault, migration 0027); receiver `crewlogic-motive-webhook` verifies HMAC-SHA1 `x-kt-webhook-signature`, attributes via `?f=` or secret-match, resolves geofence names, stores to `geofence_alerts` (0028) with franchise-scoped RLS + realtime (0029); dashboard **Live Alerts** rail (desktop) + mobile **📡 Truck Alerts** home card. (b) **JOB arrive/leave** (option 3 — a Motive geofence per job): `crewlogic-geofence-create` (circular create + delete via `POST /v1/geofences/circular`, id at `motive.geofence.id`), `job_geofences` map (0030), `crewlogic-job-geofence-sync` creates "Job Site" geofences (75m) for today's WOs (geocoded, idempotent, skips-done); receiver matches geofence_id→job → `job_arrive`/`job_leave` "🚚 \<truck\> arrived at/left \<client\>". **Multi-truck-safe: a truck EXIT is an alert only, never a delete** (trucks arrive/leave/return/dump-and-return, any order). **Delete only on Vonigo-done** (status 164/165 or label 245/9996/9993) **+ EOD sweep**; NOT on drop-from-today's-list (transient API hiccup would wrongly wipe active geofences — owner-endorsed). **Crons LIVE on dev** (pg_cron+pg_net, migration 0031): sync `*/30 * * * *`; eod-sweep `30 2 * * *` (≈10:30 PM EDT; #90 ET — per-franchise EOD timing is the multi-tenant TZ follow-up). Real-truck shakedown pending (tomorrow). Disable: `select cron.unschedule('crewlogic-job-geofence-sync');` + `...-eod-sweep`. **Prod cutover still needs:** prod migrations 0027-0031, prod fn deploys, the owner's Motive webhook re-pointed at the prod receiver + secret. Open: ignition on/off killed in Motive (owner); dashcam-disconnect + fault-code events auto-captured generically (refine labels when first seen); optional live "N trucks on site" count deferred.

> **Prod version — 2026-06-30 (v5.48.6): PDF improvements.** prod (`origin/main` = `72adf87`) serves **v5.48.6** — FRONTEND-ONLY (dev→main ff-merge; no edge-fn/migration). (1) **v5.48.5** — proposal PDF now stamps the **estimate ID + franchise ID** in small type on the last page (so a PDF traces back to its `estimate-photos/<franchise>/<id>/` storage folder — closes the traceability gap that made the lost-Bibee-estimate photo hunt necessary). (2) **v5.48.6** — **Volume Detail** page body font bumped 9pt→12pt (line/row height scaled so wrapped rows don't overlap; header label row unchanged). Owner-requested.

> **Prod version — 2026-06-30 (v5.48.4): ESTIMATES DESK shipped to prod.** prod (`origin/main` = `2bf7934`) serves **v5.48.4** (dev→main ff-merge + `crewlogic-dispatch` deployed; no migration). New desktop **📋 Estimates Desk** (Home tile; desktop ≥1024 + Vonigo-gated): estimates list (last 5 + search/filter) left, live schedule board right; click an estimate → full editor in a wide pane beside the board (relocated `#estimateEditorScreen`, restored via `hideAll` so the normal estimate flow is unaffected); full-bleed framing; Home/Back nav. **Double-click an open slot → opens that day's Vonigo schedule grid** (`Grid_Main.aspx?dayID=&viewType=1`) in a new tab (the scheduleID/reserve approach was abandoned — Vonigo mints scheduleID when you click the slot in its UI). **5-min auto-refresh + per-board 🔄** on all schedule boards (dispatch + desk). **Multi-appointment cancel UX:** Cancel on a job with >1 active appointment shows an up-front "N appointments — cancel the one in Vonigo" popup w/ an **Open Vonigo schedule** button (count from board `apptCount` = WO `countWorkOrders`); single-appointment jobs cancel normally; server-side guard remains the backstop. `crewlogic-dispatch` now carries `apptCount` mapping + the dormant `reserveSlot` action. **NOT shipped:** Chunk 4 (iCal calendar lane) — deferred. Per-appointment cancel still parked pending Vonigo support (see hotfix note below). Verified prod fn (boardGrid OK, apptCount present); prod serves v5.48.4.

> **PROD HOTFIX — 2026-06-29 (cancel guard, backend-only):** Destructive bug — cancelling ONE appointment in CrewLogic cancelled the ENTIRE Vonigo job + ALL its appointments (cancel = `/data/Jobs/` method 4 by jobID). For multi-appointment jobs (Vonigo "copy to another day" → 644660-1/-2/-3… share one jobID, each a distinct WorkOrder) this wiped all of them. **Hotfix deployed to prod `crewlogic-dispatch` ONLY** (no `dev→main`; the in-progress Estimates Desk stays out of prod): a **guard** (`countActiveAppointments` via `/data/Jobs/{jobID}` `countWorkOrders`) — if a job has **>1 active appointment, BLOCK the cancel** + message "cancel the specific appointment in Vonigo"; single-appointment jobs cancel as before. Prod frontend (v5.47.89) already surfaces the error. Verified in prod (job 852415 = 5 appts → blocked, nothing cancelled). **Follow-up — PARKED pending Vonigo support ticket (Owner filed 2026-06-29):** true per-appointment WO-cancel is NOT achievable by API probing — `/data/WorkOrders/` method 4 (the delete verb; WO reads `isCanDelete:true`) rejects every payload tried with `-600 Data validation failed`; method 2 edit-status returns errNo:0 but silently no-ops. Required fields undiscoverable by guessing. Owner opened a Vonigo support ticket for the correct cancel-appointment API call; when they return the payload/endpoint, wire it into both cancel paths (cancel by woID) + frontend passes woID, replacing the block for the woID-known case. Until then the **guard is the correct shipped fix**. dev stays at **v5.48.2** (no v5.48.3 — all per-appointment probe attempts reverted clean). Note: prod `crewlogic-dispatch` now also carries the dormant `reserveSlot` action (Estimates-Desk double-click; not called by the prod frontend).

> **Prod version — 2026-06-28 (batch 6, v5.47.89):** prod (`origin/main` = `4715a9b`) serves **v5.47.89** — **weather alerts for the ops manager** (+ the Job Plan now flags them). FRONTEND add (v5.47.89): the **Job Plan leads with a short weather line** — `⚠️ WEATHER: <event> (until <expires>) · +N more` — on the dashboard Generate-plan popup AND the standalone Job Plan screen, included in **Copy all**; uses LIVE alerts (not the saved plan) since weather is time-sensitive. (v5.47.88, the base:) new **`crewlogic-weather`** edge fn (on prod) — active **NWS** alerts (free, keyless; server-side User-Agent) for the franchise's state (`cost_settings.officeState`; drops marine/coastal noise; most-severe first). Dashboard shows a compact **⚠️ <event> +N** chip in the trucks bar next to Generate plan (color-coded); click → popup with event/expires/area/headline/safety instruction; hides when none active. No migration; no API key/cost. State-level for now (can narrow to the office point later); dashboard + Job Plan (not yet the trucks screen). Verified prod fn (#90→MA→Extreme Heat Watch). Owner-confirmed on dev. New **`crewlogic-weather`** edge fn (deployed to prod) — pulls active **NWS** alerts (free, keyless; requires a server-side User-Agent) for the franchise's state (from `cost_settings.officeState`; drops marine/coastal noise; most-severe first). Dashboard shows a compact **⚠️ <event> +N** chip in the trucks bar next to Generate plan (color-coded by severity); click → popup with each alert's event/expires/area/headline/safety instruction; hides when none active. No migration; no API key/cost. State-level for now (can narrow to the office point later); dashboard-only (not yet on the trucks screen). Verified prod fn (#90→MA→Extreme Heat Watch). Owner-confirmed on dev.

> **Prod version — 2026-06-28 (batch 5, v5.47.86):** prod (`origin/main` = `1117943`) serves **v5.47.86** — FRONTEND-ONLY (no edge-fn/migration). (1) **Measure-distance result** now renders in the fixed in-map box (was a Leaflet popup that clipped off the top edge). (2) **📋 Generate plan** button on the dashboard trucks bar (next to Find disposal) → generates the Job Plan for the **selected** day (`_trucksJobDay`→`dayOffset`) and shows it in a **scrollable popup** (route cards + Copy all + Cancel), staying on the dashboard; reuses `crewlogic-job-plan` + still upserts to `job_plans` (best-effort). (3) **Board job tooltip (#mjTip) scrolls** instead of overflowing the window: Summary + Item(s) List each cap at 140px with their own scroll (rows + Set duration + Cancel stay visible); whole tooltip caps at viewport height as a safety. Owner-confirmed on dev.

> **Prod version — 2026-06-28 (batch 4, v5.47.82):** prod (`origin/main` = `0517c62`) serves **v5.47.82** — dispatch-map polish (FRONTEND-ONLY; no edge-fn/migration). (1) **Wrong-job bug fix:** the board keyed `_mjBoardByID` by JobID, but Vonigo "copy to another day" makes appointments that SHARE a JobID (distinct WorkOrders) — so resize/duration hit the wrong copy. Now keyed by **woID** (unique per appointment); hover/resize/duration identify by woID; displayed `#` + phone lookup stay JobID (move already used woID). (2) **In-map hover box:** job (hover) + truck (click) details now render in a FIXED panel docked top-left INSIDE the map (scrolls for long item lists) instead of a Leaflet popup — the map no longer pans or clips off-edge. (3) **Popup auto-pan off** on remaining map popups. (4) **Readability:** lightened the box content colors (links `#6db3ff`, item text/price) + bg `--bg-card`; address + nearest-truck line readable. (5) **Site markers** (disposal/recycling/donation) = white circle + colored ring + emoji (were a solid colored blob). Owner-confirmed on dev.

> **Prod version — 2026-06-28 (batch 3, v5.47.77):** prod (`origin/main` = `ed608cd`) serves **v5.47.77** — **"Find disposal" shipped to the Dispatch dashboard.** Deployed `crewlogic-route-disposal` to prod (additive: returns site lat/lng + geocodes coord-less facilities; standalone disposal screen unaffected) + dev→main (v5.47.70→.77). No migration. Feature: on the dashboard trucks map, **Find disposal** → tap a truck (origin) + tap a job (endpoint) + global **% full** slider (default 100) → calls the existing `crewlogic-route-disposal` engine, plots **truck→dump→job** routes (🟢 cheapest / 🔵 fastest / 🟡 same-site / ⚪ closed-gray) with the winning dump marked, and shows **compact cheapest/fastest cards in a right-hand column beside the map** (cost·time·arrival on one line, Directions under the late-risk pill, 💰 cheaper / ⚡ saves badges, breakdown). All-closed days list the closed sites + reason. Settings guard if facilities/cost-settings missing. Map height trimmed 55vh→42vh so the screen fits without scrolling; results column matches + map `invalidateSize` on show/hide. **Also fixed (whole trucks map):** measure-distance now fires on **click** (desktop) / first-tap (touch), not hover. Desktop-only; no fit-check/volume/material-split (deferred to the route-optimization feature). Owner-confirmed on dev. Open: owner customer-E2E on prod. Verified post-deploy: prod route-disposal returns coords + app.crewlogicai.com=v5.47.77.

> **Prod version — 2026-06-28 (batch 2, v5.47.70):** prod (`origin/main` = `ac1c5b6`) serves **v5.47.70** — second dispatch batch promoted (v5.47.65→.70, deployed `crewlogic-dispatch` again — additive prompt change, no migration). Added: **Cancel-job button** on the job tooltip (Category/Reason/Notes dropdowns → same `execute kind:'cancel'` path); **unified command conversation popup** (You/AI dialog incl. clarifying questions in one popup, own mic+input, Confirm disabled until the AI resolves an action — then it lights up; for cancel it shows the reason dropdowns); **+3-days look-ahead now zooms to fit** the dots (trucks-centered); board **auto-refreshes after every write** (move/cancel/duration on both surfaces). Owner-confirmed on dev before promote. Open: owner customer-E2E on prod; (Manage Jobs voice bar still uses its panel — convo popup not mirrored there yet). 

> **Prod version — 2026-06-28 (batch 1):** prod (`origin/main` = `9e9cf35`) served **v5.47.65** — **DISPATCH DASHBOARD + TRUCKS/SCHEDULING SHIPPED TO PROD & LIVE (#90).** Promoted dev→main (v5.47.12→.65, 52 commits) + deployed `crewlogic-dispatch` to prod (the ONLY edge-fn changed; **additive** — boardGrid sequence-order/route-hours-axis/items/price + duration engine; **no DB migration**). **Live:** desktop **Dispatch dashboard** (two route-aligned schedule boards source+dest day + trucks-centered live map; cross-day move by **drag-to-slot AND voice/tap** through one source→dest confirm modal, reusing the lock→WorkOrders method-16 engine); **Manage Jobs board** upgrades (all active routes in Vonigo **`sequence`** order, **raw-open-time** 3-layer grid (30-min, matches Vonigo — not 120-min-fit), route-hours time axis, timeline auto-center on now [board only, not dashboard], job tooltip = total $ + 💬 Text SMS + clickable Maps address + **Item(s) List**); **duration change** (tap-select 0.5–5h + desktop right-edge drag-handle, two-step staged resize via the proven engine); **Where Are My Trucks** (calendar **date picker** any-day, **+3-day look-ahead from the selected day**, **trucks-centered** map [symmetric bounds keep jobs visible], measure-distance [first-tap on mobile], clickable Maps addresses everywhere). Mobile fixes: touch-sticky job popup (the duration-tap bug), capture-phase delegation. Verified post-deploy: prod `boardGrid` (EST-first sequence + boardStartMin/EndMin 360/1200 + items) + app.crewlogicai.com=v5.47.65 + marketing 200. **Billing unchanged** (still DORMANT — dev-only `BILLING_ENABLED`). **Open:** confirm `dispatch_audit` table on prod (move/duration audit log — best-effort, non-blocking; a test move confirms it records) + owner customer-E2E on prod. Plan: `docs/plan-dispatch-prod-migration.md`. Last verified 2026-06-28.

> **Prod version — 2026-06-24:** prod (`app.crewlogicai.com` / `origin/main` = `34a1d66`) serves **v5.47.4** — **ROUND-1 BILLING SHIPPED TO PROD, DORMANT.** Master switch `BILLING_ENABLED` (index.html ~3866) = `IS_DEV_ENV` → **dev ON, prod OFF**; prod is visually unchanged (paywall = original contact card; no Subscribe/Manage-billing). Deployed: `crewlogic-billing` fn to prod (inert — ping confirms hasPrice/hasSecret/hasWebhook all false); dormant frontend merged dev→main. **OWNER TO COMPLETE SETUP:** (1) apply migration **0025** to prod via the **dashboard SQL editor** (idempotent ALTER+INDEX — `db push` is UNSAFE: prod migration history out of sync, would re-run 0009–0025 incl. the 0013 backfill); (2) **LIVE-mode** Stripe (product/$29.99 price/keys/webhook at the prod fn URL) → set `STRIPE_SECRET_KEY`/`STRIPE_PRICE_ID`/`STRIPE_WEBHOOK_SECRET` on **crewlogic-prod**. **ACTIVATION (when ready to advertise):** flip `BILLING_ENABLED=true` (one-line + deploy); later `ENFORCE_TRIAL=true` after auditing prod statuses. Round-1 verified end-to-end on dev. Checklist: `docs/stripe-setup.md`. _(Prior `e20d7ef` v5.46.31 — **PRICING HOTFIX**: the customer **PDF** now honors per-truck **volume rate overrides** (`est.volumeRateOverrides`). Bug (prod estimate 1781003688449, DiSilvia): a $950 full-truck override was honored in the live total, saved `total_price` ($5,474), and the **Vonigo** submission (all override-aware), but `generateEstimatePDF` re-derived volume from the price-book rate → the **customer PDF undercharged**. Fix = PDF uses `pdfRateFor()` (override-first, mirrors `volumeRateFor`) + the live engine's tier rounding, so PDF == on-screen == Vonigo. Cherry-fixed to main as v5.46.31 (the v5.47.x **billing/Stripe self-serve** work stays on **dev**, not prod-ready). New guardrail: `~/.claude/rules/pricing-change-full-path-verification.md` (verify every pricing change across UI total / breakdown / saved record / PDF / CRM submit / cost analysis). _(Prior main = `6abe9d9` v5.46.30, Manage Jobs **Vonigo-parity + label-based done** batch v5.46.28–.30):_ (1) **done→gray rule now keyed to the Vonigo LABEL (field 201)**, not route — gray when status Completed/Archived OR label∈{245 Est Completed Job, 9996 Est Completed Est.Only, 9993 Lost}; Converted (9975/9970) + National Account (9981) stay active until Archived (board + trucks map); (2) **board pills mirror Vonigo** — BODY colored by label (global colors), LEFT STRIPE = pricing **zone** (zone from the WO `zone` relation; per-franchise zone colors are NOT API-readable so the stripe uses a stable CrewLogic palette keyed to zone id, with the real zone name in the tooltip), route = the lane; (3) **voice/text commands are day-aware** — they default to the day tab you're viewing (`_mjBoardDayID`→command `dayID`), naming a day still overrides. `crewlogic-dispatch` listRouteJobs now returns labelOpt/zoneID/zoneName/labelDone; `crewlogic-todays-workorders` returns labelDone. Full Vonigo status(181)+label(201) code map + zone IDs saved to memory `vonigo-status-and-label-codes`. _(Supersedes v5.46.26 Where-Are-My-Trucks parity below, which remains live:_ completed/archived jobs stay on the trucks map as **gray** house markers (✓ Completed) instead of dropping off — `crewlogic-todays-workorders` geocodes completed jobs too + filters "Cancelled - Today" 163; matches the Manage Jobs board. On top of the v5.46.20–.25 board-polish batch: gray completed jobs + job-ID pills + 2-line compact pills + narrower hours + half-hour gridlines + working calendar (front) + weekday-only chips + full-name tooltip + "?" help popover; `crewlogic-dispatch` keeps+flags completed / hides only cancelled / declines writes on completed jobs — completed jobs can't be moved/resized via API (Vonigo -2213 "cancelled or archived", web-UI-only); on top of v5.46.19 Google account-chooser auth fix + franchise-tz day logic + migration 0024 `dispatch_audit` + non-ET officeTimezone backfill #54/#109). _(Prior: v5.46.19 auth fix; v5.46.16 dispatcher batch; 2026-06-19 v5.45.24 trucks-map UX.)_ _Earlier sweep note (2026-06-11, v5.30.3 era retained for history):_ prod served **v5.30.3**; the deployed `index.html` is byte-identical to the working tree (the only dev-ahead commits are docs/marketing). All backlog rows previously flagged "pending deploy / DEPLOY ASAP / promoting to prod" through v5.30.1 were **confirmed LIVE in prod** (frontend greps + prod edge-fn download + prod `information_schema`). Estimate-charges **normalization confirmed still dev-only** in prod (`estimate_charges` table + dual-write absent), though its **charge-wipe 409 guard IS live** in the prod `crewlogic-estimate` fn. (1) **done→gray rule now keyed to the Vonigo LABEL (field 201)**, not route — gray when status Completed/Archived OR label∈{245 Est Completed Job, 9996 Est Completed Est.Only, 9993 Lost}; Converted (9975/9970) + National Account (9981) stay active until Archived (board + trucks map); (2) **board pills mirror Vonigo** — BODY colored by label (global colors), LEFT STRIPE = pricing **zone** (zone from the WO `zone` relation; per-franchise zone colors are NOT API-readable so the stripe uses a stable CrewLogic palette keyed to zone id, with the real zone name in the tooltip), route = the lane; (3) **voice/text commands are day-aware** — they default to the day tab you're viewing (`_mjBoardDayID`→command `dayID`), naming a day still overrides. `crewlogic-dispatch` listRouteJobs now returns labelOpt/zoneID/zoneName/labelDone; `crewlogic-todays-workorders` returns labelDone. Full Vonigo status(181)+label(201) code map + zone IDs saved to memory `vonigo-status-and-label-codes`. _(Supersedes v5.46.26 Where-Are-My-Trucks parity below, which remains live:_ completed/archived jobs stay on the trucks map as **gray** house markers (✓ Completed) instead of dropping off — `crewlogic-todays-workorders` geocodes completed jobs too + filters "Cancelled - Today" 163; matches the Manage Jobs board. On top of the v5.46.20–.25 board-polish batch: gray completed jobs + job-ID pills + 2-line compact pills + narrower hours + half-hour gridlines + working calendar (front) + weekday-only chips + full-name tooltip + "?" help popover; `crewlogic-dispatch` keeps+flags completed / hides only cancelled / declines writes on completed jobs — completed jobs can't be moved/resized via API (Vonigo -2213 "cancelled or archived", web-UI-only); on top of v5.46.19 Google account-chooser auth fix + franchise-tz day logic + migration 0024 `dispatch_audit` + non-ET officeTimezone backfill #54/#109). _(Prior: v5.46.19 auth fix; v5.46.16 dispatcher batch; 2026-06-19 v5.45.24 trucks-map UX.)_ _Earlier sweep note (2026-06-11, v5.30.3 era retained for history):_ prod served **v5.30.3**; the deployed `index.html` is byte-identical to the working tree (the only dev-ahead commits are docs/marketing). All backlog rows previously flagged "pending deploy / DEPLOY ASAP / promoting to prod" through v5.30.1 were **confirmed LIVE in prod** (frontend greps + prod edge-fn download + prod `information_schema`). Estimate-charges **normalization confirmed still dev-only** in prod (`estimate_charges` table + dual-write absent), though its **charge-wipe 409 guard IS live** in the prod `crewlogic-estimate` fn.

---

## Features — Standalone / No-CRM Mode (`docs/Feature-StandaloneMode/`)

| Project | Spec | Build | Prod rollout | Version | Last verified | Open items |
|---|---|---|---|---|---|---|
| Stage A: Native Pricing + Customers | [CL-SPEC-001](../docs/Feature-StandaloneMode/CL-SPEC-001_StageA_Pricing_Customers.md) | **Done** | Deployed (`crewlogic-pricing` ACTIVE; **no native tenants in prod yet** — owner-confirmed 2026-06-02, so prod is code-ready and awaiting first native user) | — | 2026-06-02 | (1) ~~native send uses "Generate PDF"~~ **DONE (verified 2026-06-13):** native emails the PDF via "📧 Email to Client" (`generateEstimatePDF('email')`, share-sheet/`mailto`); (2) 4 Route-Optimizer/Storage quick-select buttons hardcode `16` cy (cosmetic); (3) **CPL per-item discounting won't work for native price books** — `CPL_CATEGORIES` (index.html ~4844) matches blocks by Vonigo canonical names (`Volume Charges`, `Additional Labor`, `Single Item Price List`, recycling surcharges); native books with other block names (dev seed uses `Volume`/`Labor`/`Surcharges`/`Single Items`) load no per-item items. **FIXED on dev (v5.38.0, 2026-06-13):** CPL now matches categories by the stable `block_type` enum for native (with the Vonigo name match kept as fallback) — `crewlogic-pricing` returns `blockType`; `CPL_CATEGORIES` carries `blockType` (volume/surcharge/labor/single_item); `getCategoryItems` + `backfillItemNames` match by it. Verified the dev edge fn returns `blockType` for all 4 native blocks; Vonigo path unchanged (zero risk). **This is a native-only differentiator — Vonigo can't do customer-specific price lists at all** (owner, 2026-06-13). Pending owner UI validation (open CPL editor for a native customer → items load → discount applies) + prod promote (deploy `crewlogic-pricing` to prod). |
| Town Price Lookup | [CL-SPEC-002](../docs/Feature-StandaloneMode/CL-SPEC-002_TownPriceLookup.md) | **Done** (dual-track; verified in-browser on dev) | **Live** (v5.22.2, promoted 2026-06-02) | v5.22.2 | 2026-06-02 | Migration 0005 applied to dev + **prod**. Native = `<datalist>` autocomplete from enriched `price_list_zips`; Vonigo = town + state dropdown scoped to `cost_settings.serviceStates`, default `officeState`; village-name not-found hint. Parked: per-estimator default state |
| Phase 2: Native Auth (Supabase Auth, invite-first) | [CL-SPEC-003](../docs/Feature-StandaloneMode/CL-SPEC-003_Phase2_NativeAuth.md) | **Done** | **Live** (login V2 shipped v5.18.0) | v5.18.0 | 2026-06-02 | ~~Magic-link only by design~~ → **email+password auth added v5.30.0** (see backlog row). Phase 3 RLS done |
| Phase 3: RLS / SEC-1 | [CL-SPEC-004](../docs/Feature-StandaloneMode/CL-SPEC-004_Phase3_RLS_SEC1.md) | **Done** — RLS enforced (cutover 2026-06-03) | **Live** (prod, v5.23.2) | v5.23.2 | 2026-06-03 | All public tables franchise/tenant-scoped (migrations 0006–0010, dev+prod) or deny-all (vonigo_*); client sends user JWT (§6); `crewlogic-link-identity` auto-links on login. Prod cutover done in order: link-identity deployed → backfilled mark (gustavo auto-links next login) → client v5.23.2 live → policies applied. Verified charles.dennis reads 48 estimates under RLS; stranger sees 0. **Owner live-verified in prod 2026-06-03** (estimate creation, cover-photo retrieval, volume estimate, price lookup — all good on a junkluggers.com login). **Follow-ups:** §8 `estimate-photos` storage scoping — `authenticated` access now granted so logged-in users can read/write photos (migration `0011`, dev+prod 2026-06-03); per-franchise *path* scoping still deferred; fuller cross-tenant test suite (§10) |

## Platform / Infrastructure

| Project | Build | Prod rollout | Last verified | Notes |
|---|---|---|---|---|
| Dev/prod Supabase separation | **Done** (Supabase layer) | Live | 2026-06-02 | prod `ozfkpxyachigfpcmvekz` + dev `bagkimfwmpwjfhfhmsrb` (`crewlogic-dev`). Frontend is still single-deploy |
| `supabase/migrations/` folder | **Done** | n/a | 2026-06-02 | Sequential `NNNN_*.sql` (`0001`–`0004`). Apply to dev first, then promote |
| Edge function source under git | **Done** | Live | 2026-06-02 | All 14 functions committed & verified byte-identical to prod |
| n8n → Edge Functions migration | **Nearly done** | Mixed | 2026-06-10 | **Only the route-optimization engine remains in n8n** (`apiFetch(N8N_BASE + '/crewlogic-route')`, index.html ~7341). `delete` + `searchClients` + `submitQuote` were migrated into `crewlogic-estimate` (direct Vonigo MD5 auth, **no OAuth** — the old "requires Vonigo OAuth" note was wrong). Photo sweep is `crewlogic-photo-sweep` pg_cron, not n8n. |
| Deploy workflow → Claude Code direct | **In progress** | — | 2026-06-02 | Repo moved to `~/code/crewlogic`; now committing/pushing from Claude Code (was Downloads→GitHub Desktop) |
| Onboarding process | **Not started** | n/a | 2026-06-02 | New todo. At signup / Vonigo provisioning, capture franchise `serviceStates` (the multi-state handful) + a per-estimator default state. Estimators (magic-link) may work in a different state than the office (e.g. estimator in CA, office in AZ) → estimator default state falls back to franchise `officeState`. `serviceStates` also owner-editable in Settings. Feeds Town Price Lookup (Vonigo track) |

## Backlog / feature requests

| Request | Status | Notes |
|---|---|---|
| **Per-facility wait times** (per-site, replacing the global wait time) | **Backlog — not started** (owner 2026-06-24) | Today a **single wait-time** value applies to ALL disposal facilities (and the same global treatment for recycling + donation sites). Owner wants the wait time set **per individual site**: an input field when setting up each facility in the **Cost settings → Facilities** section, so every disposal / recycling / donation site carries its own wait time. Likely: add a `wait_time` (minutes) column to the `facilities` table (migration 0023 set) + a per-site input in the facility editor UI; update the **Route Optimizer / Job Router / cost-analysis** disposal-time math to read the per-site wait instead of the one global value. Backfill the existing global value as each site's default so nothing changes until edited. |
| **Real route optimization** (minimize per-route drive time across booked jobs) | **Future — brainstorm before building (do NOT build yet)** (2026-06-21) | Owner pipeline idea, distinct from the disposal Route Optimizer (this is NOT where to dump trucks). Goal: across the day's booked jobs spanning multiple routes, **re-combine jobs to minimize drive time per route**. Example (Mon 6/22): a job in Narragansett RI on R4 + one in Eastham MA on R3 = huge drive gap → would NOT want them on the same route; might move a New Bedford estimate from R1 to 11:30 on R4 — but that requires **customer conversation first** (so it's *suggest*, not auto-apply). Needs: per-job **geocoded locations** + times + durations + drive-time matrix (Google Distance Matrix — already used in `crewlogic-estimate` calcDistances), then an optimizer that proposes route/slot reassignments for human approval, executed via the **same move primitive** (lock + WorkOrder method 16). **PLUMBING IMPACT ON CURRENT VOICE-DISPATCH BUILD (act on now):** build move/availability/job-read as **reusable server-side tools** (not buried in the voice handler), and have the job-read tool return **geocoded lat/lon + duration** — so the future optimizer reuses them. Brainstorm a real plan later. |
| index.html size / architecture review (curb bloat before adding more) | **Discussion queued — after Voice Dispatcher P0/P1** (2026-06-21) | Owner: the single `index.html` is ~22k lines (CLAUDE.md still says ~18.4k — drifted) and growing; **discuss whether to change the approach before more features add bloat.** Decide deliberately: keep single-file (constraints: no build step, CDN libs, `hideAll()`/`allScreens` screen model) vs. introduce a light split/build. Options to weigh: (a) status quo + discipline; (b) split JS into modules + a tiny bundler (adds a build step — big change to the deploy flow); (c) extract large self-contained blocks (signs, volume-check, settings) into separate files concatenated at deploy. **Constraint:** the dev→main Cloudflare Pages flow currently deploys the raw file with NO build — any split must preserve or consciously replace that. Hold until the dispatcher feature lands, then strategy-mode discussion. |
| **Voice AI Dispatcher** (move/cancel Vonigo jobs by voice) | **Plan drafted — awaiting owner approval** (2026-06-20) | Speak "move the 9am Route 1 job to Thu 3pm" / "cancel job 2 on Route 3" → AI clarifies → always-confirms → executes move/cancel in Vonigo. **Plan: `docs/plan-voice-dispatch.md`.** Locked decisions: move+cancel in v1 · browser mic (Web Speech) · always-confirm. **Vonigo recon DONE & validated** (see memory `vonigo-routes-availability-api`): routes, jobs+durations(field 186), availability (open slots), **zip-zoning confirmed** (availability+zip → dedicated routes; no zip → owner-override all routes; availability already excludes closed+booked), **cancel proven** (`/data/Jobs` method 4, fields 974 category/975 reason/973 comments; soft status-change not delete). **Open to validate (P0 spike):** MOVE write path (WorkOrder method-2 edit of date 185/time 9082/route — proven on Jobs, not yet WO appt fields; test on cleared RE-SCD); full cancel reason picklist (974 categories + 975 reasons per category). Architecture = edge fn `crewlogic-dispatch` (server-side Claude tool-agent, franchise Vonigo creds) + audit log + dry-run. **Supersedes/expands the "Reschedule a job's appointment from inside CrewLogic" row below.** **UI increment v5.46.6 (2026-06-21, dev — frontend-only):** added a **date strip** below the Manage-Jobs header — quick-pick chips for the next 5 days (Today/Tomorrow/weekday+date) + a 📅 calendar picker; tapping a date lists **all routes'** jobs for that day via the deterministic `listRouteJobs` action (no AI call), rendered through the **same** `mjRenderResult` 'jobs' collapsible card the AI produces. Also pushes the same conversation memory so a follow-up voice command ("move the 2nd job to Route 3") resolves against the listed jobs. **v5.46.7 (2026-06-21, dev):** (1) **auto-loads today's jobs on screen entry** (Today chip selected); (2) replaced the flat list (for the date-driven path) with a **schedule board** — CrewLogic take on the Vonigo mobile day view: **route lanes** (rows) × **time axis** (hour columns, horizontal scroll, sticky route labels), colored job blocks positioned by start time + width=duration, numbered per-route, tap a block → detail (address/duration/route/status + lazy phone + Vonigo link) below the board. Only routes with jobs get lanes (URGENTCB/completed already excluded by `listRouteJobs`). Conversation memory now grouped **by route** with per-route position #s so "job N on route X" / "that <town> job" resolve. The AI voice 'jobs' list (route-specific spoken queries) still renders the original collapsible list. **v5.46.8 (2026-06-21, dev):** board block subtitle changed from time→**City · zip** (time is derivable from the timeline). Added a **job tooltip** — desktop **hover** (quick peek) + mobile **tap** (sticky; × or tap-outside to dismiss) — showing **ID, Subtotal, Street, City, Zip, Phone (lazy), Vonigo link**. Subtotal = WorkOrder field 813 (`price`), added to `listRouteJobs` (crewlogic-dispatch **deployed to dev**); Street/City/Zip parsed client-side from `address` (`mjAddrParts`). **Stage-2 follow-up (not built):** Vonigo card's **Summary** (Job field 978) + **Item(s) List** (booking items) need a per-job Job-object fetch + an item-field-ID probe — lazy-load into the same tooltip; owner asked for these (excluding name/status/scheduled-time/zone). Pending dev browser test (#90 owner). **v5.46.9 (2026-06-21, dev): DURATION change by voice/text** — "change the Route 1 estimate tomorrow to one hour" / "shorten job 2 to 90 minutes". New `duration` intent in the Claude agent (resolves the exact job, keeps its CURRENT route/day/start, parses the new minutes) → always-confirm card ("Duration: 120 min → 60 min · MA1REG · 9:00 AM (unchanged)") → execute. **Write primitive = the SAME proven move path** (re-lock the same slot at the new length via `/resources/availability/` method 2, then `/data/WorkOrders/` method 16) — slot unchanged, only duration differs; audited as action `duration`. Shortening is always safe; lengthening fails cleanly if the longer slot isn't open ("slot not open that long"). crewlogic-dispatch **deployed to dev**. **v5.46.10 (2026-06-21, dev):** confirmed on dev that the same-start re-lock IS rejected ("could not set the new duration / slot not open") because the job occupies that instant — so the duration write was **switched to a direct field edit**: `/data/WorkOrders/` **method 2 (Edit), Fields:[{fieldID:186, fieldValue:<minutes>}]** (the same edit pattern proven on the National Accounts summary write; no lock, slot unchanged). Frontend error fallback now also surfaces `data.edit.errMsg`. crewlogic-dispatch redeployed to dev. **v5.46.11 (2026-06-21, dev) — DURATION CHANGE WORKS (owner-verified "works perfectly"):** the WO duration field is scheduler-managed (method-2 edit silent no-ops; same-start re-lock `-600`), so the resolved write is a **two-step move** that drives field 186 via the lock's duration param: (A) lock+method16 to a free staging slot on the same route at the new duration (sets 186, frees the original slot), (B) lock+method16 the original start (now free) at the new duration → job back in place, new length. Probe-verified on WO 985290; mechanism logged in memory `vonigo-routes-availability-api`. Temp probe/dump diagnostics removed from the fn. **Also v5.46.11: push-to-talk mic fix** — desktop Web Speech was auto-submitting on a detected pause (`continuous=false` default + submit-on-final); now `continuous=true` and the command is submitted only when the user taps the mic to stop (mobile single-shot still works via `onend`). **v5.46.12 (2026-06-21, dev): move + duration in one command** — "move it to Route 3 and make it an hour" → a SINGLE move that books the target slot at the new length (the move's lock already sets field 186; no separate two-step needed since the target slot differs). AI sets move plan `durationMin`=new + `fromDurationMin`=current; checks slot-fit against the NEW duration; confirm card shows a "Duration: 120 min → 60 min" row when changed. Same-slot duration-only still uses the two-step `duration` intent. Pending: full owner pass on dev, then prod promote (gated). **v5.46.13 (2026-06-21, dev): user-facing error messages (owner-approved design).** Two layers: **(L1 prevent+offer)** when a move's requested time isn't open for the needed duration, the AI checks if a SHORTER length fits there and OFFERS to move+shorten in one go ("Route 3 at 9 only has 1 hour open — want me to move it and shorten it to 1 hour?"); else offers nearest fitting times. **(L2 graceful execute errors)** raw Vonigo errors are `console.error`'d server-side and NEVER returned to the client (per no-internals-to-client); failures return safe actionable strings via a `data.error` field the frontend already shows: move-slot-not-open → "That time isn't open for the full N — pick another slot or shorten the job to fit"; move/cancel commit fail → friendly retry; duration lengthen can't reclaim original → "Can't extend to N at TIME — something's booked right after… (job temporarily at STAGING — adjust in Vonigo)"; cancel reason not available/configured → "pick another reason"; Vonigo auth/unreachable → "Couldn't reach the scheduling system"; top-level catch → generic safe message. New `durLabel()` helper for friendly lengths. More messages added as they surface (owner). **v5.46.14 (2026-06-22, dev): tooltip Summary + Item(s) List** — both turned out to be on the **WorkOrder** already (no per-job Job fetch needed): **field 200 = Summary**, **field 10336 = Item(s) List** (Job field 978 mirrors 200). Added to `listRouteJobs` (crewlogic-dispatch, deployed dev) + tooltip renders them as stacked blocks preserving line breaks; tooltip capped `max-height:70vh; overflow-y:auto`. Dev-verified the fields populate for job 855374. Completes the tooltip ask (ID/Subtotal/Street/City/Zip/Phone/Summary/Item(s) List/Vonigo link). **v5.46.15 (2026-06-22, dev, frontend-only): tooltip polish + now-line.** Tooltip: the **Job ID is now the clickable Vonigo link** (label "Job", value = jobID 🔗), removed the separate bottom "Open in Vonigo" link, and **dropped the Subtotal row** (price field still returned by listRouteJobs, just not shown). Schedule board: added a **current-time vertical line** (thin red line + dot) that draws only when viewing **today** and the clock is within the board's visible hours (`mjDayID(now)===dayID`, device-local — fine for ET #90). **v5.46.16 (2026-06-22, dev): Manage Jobs card gating widened** — was `role==='owner' && Vonigo`; now **any user on a Vonigo-connected franchise** (owners AND estimators/guests/testers) per owner 2026-06-22 (`manageJobsCard` index.html ~6773 → `_vonigoMJ` only). **🚀 PROMOTED TO PROD 2026-06-22 (v5.46.16, owner-authorized "go") — LIVE.** Sequence completed: (1) migration 0024 applied to prod (relink→`db query -f`→relink-back; `dispatch_audit` verified present, 0 rows); (2) `crewlogic-dispatch` deployed to prod; (3) `crewlogic-ai` deployed to prod (scale-anchored volume prompt rode along); (4) `dev`→`main` ff-merge pushed (main==dev==`84d8dbe`) → Cloudflare serving app.crewlogicai.com **v5.46.16 (watcher-confirmed live)**. **Prod backend smoke PASSED:** prod `crewlogic-dispatch` listRouteJobs (franchise 90, today) → 4 jobs incl. summary+items. Available to any Vonigo-connected user (owners + testers). `crewlogic-vonigo-probe` intentionally NOT deployed to prod. **Whole batch LIVE: board+date-strip+now-line, tooltip (Job-link/Street/City/Zip/Phone/Summary/Items), move/cancel, duration two-step, move+duration combo, push-to-talk mic, friendly errors.** Open follow-ups: live move/cancel/duration WRITE not yet exercised in prod (read path verified — owner to run a real command when ready); **multi-tenant tz is a REAL latent bug NOW (corrected 2026-06-22 from prod roster), not a future-onboard concern** — of the 7 Vonigo franchises, **#54 TX–El Paso = MOUNTAIN and #109 OR–Willamette Valley = PACIFIC** (the other 5 — #90 MA, #36 VA, #116 OH/Derek, #28 CT, #31 NY — are all Eastern). The dispatcher's day window (`easternDayID`) + the now-line (device clock) lean Eastern, so "today's jobs"/now-line can be wrong for #54/#109 (both can now open Manage Jobs after the gating widen). **Gotcha:** `cost_settings.officeTimezone` is NULL for ALL franchises, so a state→tz map resolves TX→Central — still WRONG for El Paso (Mountain); #54 needs an explicit `officeTimezone`. Fix = apply `resolveTimezone()`/`Intl` (from `crewlogic-route-disposal`) to the dispatcher day logic + drive the now-line off franchise tz, and backfill `officeTimezone` (esp. #54). **CODE FIX LANDED ON DEV v5.46.17 (2026-06-22):** `crewlogic-dispatch` now resolves the franchise tz (`franchiseTz()` → `cost_settings.officeTimezone` → `STATE_TZ[officeState]` → ET) and computes "today"/the day-table via `dayIDInTz(tz)` (was hardcoded `easternDayID`); frontend now-line + date chips use `mjFranchiseTz()`/`mjNowParts()` off `currentUser.costSettings`. `dayEpoch` (Vonigo naive-Eastern WO date-field convention) intentionally unchanged. **6/7 franchises now correct automatically** (5 ET states + #109 OR→Pacific via state map). **STILL NEEDS DATA BACKFILL: #54 El Paso `officeTimezone='America/Denver'`** — TX state-map gives Central (wrong); awaiting owner confirm before the prod+dev write. **BUGFIX 2026-06-22 (edge-fn, dev+prod LIVE): cancelled/archived jobs still showed on the board.** Owner saw a cancelled Route-1 job (Domina-Reyes, Vonigo status **'Cancelled - Today'**) still listed. Root cause: `listRouteJobs` only excluded `statusOptionID===162` ("Cancelled"), but a SAME-DAY cancel gets the distinct status **"Cancelled - Today"** (different optionID) and **"Archived"** also slipped through — **NOT caching** (the server was returning them; the board re-fetches each open). Fix: also drop non-active jobs by status TEXT `/cancel|archiv|complet/i`. Verified server returns 4 (was 6) on **dev AND prod**. `crewlogic-dispatch` deployed dev + prod; dev→main synced. **v5.46.20 (2026-06-22, dev): board refinements (owner req)** — (1) **Vonigo job ID on every pill** (3rd line `#<jobID>`); (2) **completed/archived jobs now KEPT + shown GRAYED** (slate `#5b6b7a`, opacity .72, ✓ prefix) instead of hidden, so a finished job stays visible (e.g. German/RI4REG after payment → 'Archived'); **only CANCELLED is hidden now** (filter split: hide `/cancel/i`+162; flag `completed=/archiv|complet/i`). `listRouteJobs` returns a `completed` bool; (3) **more compact board** (ROW 50→46, PXH 100→82, LW 58→52, 20px header, smaller fonts) to show more timeline. Dev-verified: 5 rows (German archived completed:true back, 4 Open, cancelled Domina-Reyes still hidden). Pending owner dev check → promote. |
| Multi-tenant time-zone correctness (generalize Eastern-hardcoded date logic) | **Not started — latent multi-tenant bug; not breaking today (#90 is ET)** (2026-06-20) | Date/calendar logic must resolve the **franchise's own IANA tz**, not assume Eastern, so the calendar is correct for ET/CT/MT/PT/AZ/Hawaii/Alaska franchises. **Canonical pattern already exists:** `crewlogic-route-disposal` (`STATE_TZ` map + `resolveTimezone(cs)=cs.officeTimezone||STATE_TZ[officeState]||ET` + `localParts(date,tz)` via `Intl.DateTimeFormat`). **Eastern-hardcoded spots to fix before any non-ET franchise onboards:** (1) `crewlogic-todays-workorders` `getEasternMidnightEpoch` + dateMode bounds (NOTE: its `Date.UTC` naive-Eastern is *correct* for Vonigo WorkOrder date FIELDS — the fix is to drive the day window from the franchise tz, not to remove the Vonigo convention); (2) `crewlogic-job-plan` ~line 276 `nowET`; (3) any frontend date display. **Rule now in CLAUDE.md** ("Time zones & dates"). **Root cause (owner-flagged 2026-06-20):** recurring Claude bug — using `Date.UTC(y,m,d,…)` for a *local* wall-clock moment lands 4-5h early; wall-clock→epoch must be TZ-aware + verified with `date -r`. See memory `eastern-epoch-conversion-discipline`. **PARTIAL FIX — PROMOTED TO PROD 2026-06-22 (v5.46.17, LIVE):** `crewlogic-dispatch` (Manage Jobs board/now-line) is now tz-aware (`franchiseTz`/`dayIDInTz` + frontend `mjFranchiseTz`/`mjNowParts`) — deployed to prod + `dev`→`main` merged (main=`670c244`); ET franchises byte-identical (zero change), non-ET now correct. The two non-ET franchises' `officeTimezone` was backfilled in prod by their real city (**#54 El Paso=America/Denver, #109 Eugene=America/Los_Angeles**; read-back verified). **Still Eastern-hardcoded (other features, to generalize next):** `crewlogic-todays-workorders` `getEasternMidnightEpoch` day-window + `crewlogic-job-plan` `nowET` — apply the same `franchiseTz`/Intl pattern. |
| **Onboarding: capture franchise location + timezone (stop guessing)** | **New — quick project (owner 2026-06-22)** | During onboarding, have the franchisee enter their full franchise **location info** (office address/city/state/zip) AND **pick/confirm their timezone**, persisted to `cost_settings.officeTimezone` (IANA, e.g. `America/Denver`) — so the app NEVER guesses tz from a state map (which is wrong for split states: TX/El Paso is Mountain, not Central). Default-suggest the tz from the city/zip but let them confirm/override. Until this ships, each new non-ET franchise needs a manual `officeTimezone` backfill (as done for #54/#109 on 2026-06-22). **Owner directive 2026-06-22:** "use the exact time zone of the city they set up… then we can stop guessing altogether." Touch points: signup/provisioning (`crewlogic-signup`/`_shared/provisionNative`) + Settings franchise/cost tab (add a tz picker). |
| Telematics partnership / referral (Motive + Linxup) | **Owner business action — not started** (2026-06-19) | Owner to **explore a partnership/referral agreement** with **Motive** and/or **Linxup**. Franchises need a GPS provider to use the Job Router + "Where Are My Trucks?", so CrewLogic is effectively funneling signups — worth a referral deal. **Build hook already in place:** the no-telematics states (Job Router truck selector + Where-Are-My-Trucks not-connected) now show **"Sign up: Motive · Linxup"** links to `gomotive.com` / `linxup.com` (v5.45.17). When/if referral URLs exist, **swap those two plain links for the referral URLs** (one-line edit each). Owner next-action: contact Motive + Linxup partner programs. |
| Reschedule a job's appointment from inside CrewLogic ("pull it forward") | **Not started — future build** (2026-06-19) | **Use case:** crew finishes Route 1, a truck is near a job that's scheduled 3 days out → call the customer, and if they say yes, move that appointment to **today** without leaving CrewLogic. **Why in-app (not a link):** Vonigo's appointment sub-page (`Job_WorkOrder.aspx?woID=`) only renders inside Vonigo's own frame — it can't be deep-linked standalone (tested 2026-06-19, v5.45.15 → reverted to the Job-page link in v5.45.16). So the only way to truly save the step is to do the reschedule in CrewLogic. **What it needs:** (1) **visibility into the jobs on that truck's route + the open time slots that day** — compute gaps from the route's existing appointments (we already pull route + times via `crewlogic-todays-workorders`); (2) a date/time picker UI on the job popup ("Move to today / pick a slot"); (3) **write the new appointment date/time back to the Vonigo WorkOrder via the API** (method 2 Edit on the work order — same MD5-auth write pattern proven in the National-Accounts summary test); (4) refresh the map/route after the change. **Origin:** trucks-map "Open job in Vonigo" work — the appointment isn't deep-linkable, so in-app reschedule is the real fix. |
| 🔴 SECURITY: live API keys committed in `docs/Route Optimization.json` | **Open — needs owner action (rotate + strip)** (2026-06-18) | The n8n export `docs/Route Optimization.json` (added 2026-06-12) embeds **live API keys** in its `Workflow Configuration` set node: **Google Maps**, **Anthropic** (×2 occurrences), and the **Motive** token. Confirmed via count-only grep (`AIzaSy`=1, `sk-ant`=2). Repo is private → per private-repo-no-history-scrub, **HEAD removal is the ceiling** (no history rewrite unless going public). **Recommended action:** (1) **rotate** the exposed Google Maps / Anthropic / Motive keys (exposure already occurred at commit time); (2) **strip the keys from the JSON or remove the file** (the routing logic has now been extracted into `docs/plan-route-optimizer-r1.md`, so the raw export isn't needed long-term). Ties into the **Google-credential-cleanup** project (same Google key family). Owner does the rotation in each console; Claude can strip/remove the file + repoint on request. |
| Route Optimizer re-architecture (multi-tenant; off n8n + Google Sheet) | **Not started — large project; blocks Route Optimizer as a paid feature** (2026-06-18) | Owner (2026-06-18): the Route Optimizer **only works for #90** and needs a full re-architecture before any other franchisee can use it. **Verified in code:** `routerCard` hard-gated to `franchiseID==='90'` + owner (index.html:6631); engine is **n8n** (`N8N_BASE/crewlogic-route`, :8472 — the last live n8n dependency); **no Google Sheet ref in app code** — that data lives in the n8n workflow under the **owner's personal Google ID**. **What the Sheet/n8n provides today:** facility **costs + hours of operation** (transfer/recycling/donation centers); **geofence wait-times** accumulated over time from **Motive/Linxup geofences** as trucks go in/out; **possibly the "volumes" tab** (truck-load space per item — owner unsure if it feeds volume "rather than AI"; in-app *estimate* volume uses the AI Photo Analyzer + price-book fractions, so the Sheet volumes likely feed the n8n route/load engine, not the core estimate — **n8n side unverified**). **Why cross-franchise is hard:** each franchisee must (1) set up their own geofences on Motive/Linxup, (2) accumulate wait-time data over time (not instant), (3) input costs + hours. **Re-architecture:** move OFF n8n + Sheet → **edge functions + Supabase tables** (per-franchise facilities, costs, hours, accumulated geofence wait-times). **Until done:** #90-only. See memory `route-optimizer-rearchitecture`. **Contrast:** Live Trucks/telematics IS already per-franchise (migration 0018) — multi-tenant-ready. **Payments impact:** Route Optimizer = **roadmap / "coming soon" / Enterprise-custom**, NOT a standard launch Pro feature. **ROUND-1 SPEC DRAFTED 2026-06-18: `docs/plan-route-optimizer-r1.md`** (awaiting owner approval) — a slimmed multi-tenant **disposal-stop recommender** built on existing `cost_settings` + cost engine (no n8n/Sheet/geofences): telematics-gated (Motive/Linxup required), starts from live truck position, ranks the franchise's `disposalSites[]` by cost + time, returns least-cost + least-time picks. Weight = **CY/16 = tons** (locked). New per-site fields: **minimumType (none/weight/dollar) + minimumValue** (above any min, always prorated by weight). New edge fn `crewlogic-route-disposal` + a NEW separate card/screen (the #90 n8n `#routerCard` stays untouched). **ALL DECISIONS LOCKED + R1 FULLY BUILT ON DEV (2026-06-18):** route = current truck location → transfer station → job (job endpoint); cost = disposal fee + **2×crewRate** labor (drive+wait) + **fuel** (we diverge from n8n which omits fuel/hardcodes $55); min model none/weight/$ (superset of n8n dollar-only); per-site hours-of-op (M-F 7-4 / Sat 7-12 / Sun closed defaults) + franchise federal+custom **holiday** list; **franchise-local timezone** for all hours/holiday/late-warning (avoid n8n's hardcoded ET); late-warning GREEN/YELLOW/RED; endpoint = today's Vonigo jobs + always "Our location"; Vonigo-only (not native). **Architecture: moved OFF the cost_settings JSON blob → relational tables** (migration `0023`: `facilities`/`facility_hours`/`franchise_holidays`, RLS service-role-only, blob-backfilled — applied to DEV). Increments on dev: v5.44.0 (disposal Settings: minimums + 7-day hours + holiday checklist) → v5.44.1 (repointed facilities Settings to tables via `crewlogic-settings`) → `crewlogic-route-disposal` engine (deployed dev, curl-verified: tz-aware, Distance Matrix live) → v5.45.0 (new Disposal Router card/screen). Specs: `docs/plan-route-optimizer-r1.md` + `docs/plan-route-optimizer-r1-schema.md`. **Pending:** owner dev review; 2 refinements (card gating → tighten to actually-telematics-connected; late-warning scheduled-time tz); then **prod promotion (gated)** — apply 0023 + deploy `crewlogic-settings`/`crewlogic-route-disposal` to prod + merge dev→main. New rule: `ask-before-json-blob-features`. **🚀 PROMOTED TO PROD 2026-06-18 (v5.45.7, LIVE):** migration 0023 applied to prod (28 facilities backfilled across franchises — real #90 sites: Raynham/Vinagro-RI/Johnston-RI/Rochester/Borne/S&J-Cape — 196 hours rows, RLS verified); `crewlogic-settings` + `crewlogic-route-disposal` deployed to prod; `dev`→`main` ff-merge pushed → Cloudflare serving v5.45.7 at app.crewlogicai.com (confirmed). Prod engine smoke-verified (6 sites, tz America/New_York, Distance Matrix live, hours filter working — all closed after 16:00 evening test = correct). **R1 increments:** v5.44.0 disposal Settings → v5.44.1 facilities→tables → engine → v5.45.0 screen → v5.45.1/.2 truck-fetch (franchiseInternalID) → v5.45.3 arrival/late-warning fix → v5.45.4 truck town → v5.45.5 12h times → v5.45.6 collapsible cards → v5.45.7 2-stop Directions button. **🚀 FOLLOW-UP BATCH PROMOTED TO PROD 2026-06-19 (v5.45.21, LIVE):** v5.45.18 holiday-aware highlighted "all sites closed" message → v5.45.19 "cutting it close" flag (truck arrives within 15 min of close, highlighted) → v5.45.20 fix: load saved `home_card_order` on **Google** login (was only on cached-session path → card order reverted on Google sign-in) → v5.45.21 fix: resolve "Our location" endpoint **server-side** from `cost_settings` (truck home/office) so Find-Best-Disposal works regardless of client session. Engine fix: **federal holidays observed by default** (prod `franchise_holidays` was empty → June 19 Juneteenth wasn't detected). `crewlogic-route-disposal` redeployed to prod (verified: Our-location resolves "2 County Road, Freetown, MA 02717"); dev→main ff-merge pushed (Cloudflare serving v5.45.21). **Remaining:** franchises connect telematics in prod Settings → Trucks; per-franchise hours/minimums review. **Round-2 ideas (not built):** geofence wait-time accrual, Google-Sheet bulk import, AI cost-vs-time recommendation, recycling/donation hours mirror. |
| Subscription field model: `subscription_tier` = product plan, not access state | **Data normalized in prod 2026-06-18; 2 code finishers folded into Payments** | Owner spotted Willamette Valley (#109) came over as franchise `subscription_tier='free'` while existing testers showed `'tester'`. **Root cause:** a 2026-06-05 hotfix (testers paywalled) stuffed the access-*state* value `'tester'` into the *tier* column — conflating "what plan" (tier) with "do you have access" (status). **Decision (owner 2026-06-18):** `subscription_tier` is the **product/billing plan** (`free`/`pro`/`enterprise` + AI allowance, for Payments); access state lives in `subscription_status` (tenant/franchise). `'tester'` is a state, not a plan. **DONE:** verified all 5 tester-tier franchises (#116/#28/#31/#36/#54) keep access via tenant `status='tester'`, then prod UPDATE `subscription_tier 'tester'→'free'` (reversible; no access change). Now consistent: testers=`free`, #90=`pro`. `subscriptionTier` is currently **write-only** in the client (loaded in 6 places, read by none) — its only live effect was the gate honoring access-values in it. **2 finishers → do with Payments:** (1) **Gate cleanup** — `buildSessionFromSupabaseAuth` (index.html ~4935) still does `if (ACCESS.indexOf(ft)!==-1) return ft`, treating tier as an access grant; once tier=plan-only, stop reading tier for access (else a future **canceled `pro`** keeps access via tier). (2) **Provisioning consistency** — `_shared/provisionNative.ts` creates franchises with `subscription_tier=null`; if `free` is canonical "no paid plan," write `'free'` there too (else `null` and `free` both mean the same). |
| Track Photo-Analyzer volume-tier adjustments (calibration data) | **Not started — analytics; plan before executing** (2026-06-18) | Owner goal: whenever a user **bumps an estimate UP a volume tier or DOWN a tier after the Photo Analyzer** (the AI volume estimate), record it, so over time we can measure the analyzer's systematic bias and **correct the volume math**. **Signal to capture:** AI-suggested truck-fraction (the analyzer's recommended tier) vs the user's **final** selected fraction → direction (up/down) + magnitude (tier delta). **Where it lives (index.html):** `analyzeEstimate` call ~4321 → AI suggestion rounded into `pendingVolumeOptions` ~12402 (`fraction`); `selectVolumeFraction` ~12514 is the user re-pick; `tierName(fraction)` ~12684 maps fraction→label (Minimum/1/8…Full Truckload); capture the delta at **save/submit** (final fraction is authoritative — user may toggle a few times). **Approach (lean):** ride the existing **`usage_events`** backbone (migration 0022, `_shared/usage.ts` `logUsage`) with a new event type e.g. `volume.tier_adjusted` `{ai_fraction, final_fraction, delta, estimateID}`, attributed to franchise+tenant — no new table needed. Alternative: a dedicated `volume_calibration` table if we want richer analysis fields. **Open decisions (plan before build):** (a) `usage_events` vs dedicated table; (b) capture only when AI was actually run (skip manual-only estimates); (c) store the AI's raw suggested volume/CY too, not just the rounded tier, for finer calibration. **Output later:** a per-tier bias report (do users consistently bump 1/4→3/8? → the analyzer under-reads at that range). Build dev-first after owner picks (a)/(b)/(c). |
| Google account-chooser after sign-out (re-do, post-revert) | **Not started — follow-up from v5.43.6 revert** (2026-06-18) | v5.43.6 added `queryParams:{prompt:'select_account'}` to the 3 Google `signInWithOAuth` calls (index.html 4778/5038/6206) so a signed-out user could pick a different Google account; **reverted in v5.43.7** because Google 400'd ("malformed, should not be retried") **after** account selection. **Root cause (confirmed by diffing the two live Supabase→Google authorize URLs):** Supabase leaks a non-standard **`redirect_to`** param into the Google authorize URL; Google ignores it on the fast path but rejects it during `prompt=select_account`'s stricter re-validation. **Candidate fixes (dev-first, MUST browser-test the full Google round-trip — curl can't reproduce):** (1) stop the `redirect_to` leak — drop `redirectTo` from the call and rely on each Supabase project's **Site URL / Additional Redirect URLs** config (dev project `bagkimfwmpwjfhfhmsrb` → dev.crewlogic.pages.dev, prod `ozfkpxyachigfpcmvekz` → app.crewlogicai.com; localhost would need adding); (2) different chooser mechanism. **Verify on dev:** signed-out → Continue with Google → chooser appears → select → lands in app (no 400) → re-promote only after owner confirms in browser. **RETRY ON DEV v5.46.19 (2026-06-22):** owner hit the loop in prod (personal non-junkluggers Google account silently picked → no workspace → "Name your workspace" → "use a different account" → signOut clears Supabase not Google SSO → same account re-picked → loop; owner wants their @junkluggers.com acct). Re-added `queryParams:{prompt:'select_account'}` to the 3 live `signInWithOAuth` calls (`loginV2Google` ~4901, `triggerGoogleSignIn` ~5161, signup ~6329; NOTE: pre-init triggerGoogleSignIn at line ~37 with prompt+hd is DEAD — overridden by the main-script def at ~5152). **Kept `redirectTo` this round** — testing empirically whether the v5.43.7 400 still reproduces (Supabase may have changed; curl can't test). **Pending owner browser test on dev** (chooser appears? select → no 400?). If it 400s → apply candidate fix (1): drop `redirectTo` + verify Supabase Site URL/redirect config per project. **✅ RESOLVED — OWNER-VERIFIED ON DEV + PROMOTED TO PROD 2026-06-22 (v5.46.19, LIVE, first-check publish).** Owner confirmed on dev ("seems to work" — chooser appears, no Google 400, junkluggers account lands in app). The `prompt:select_account` 400 did NOT reproduce this round (Supabase behavior changed since the v5.43.7 revert), so `redirectTo` was kept. Fixes the prod login loop; account chooser now shows on every Google sign-in. |
| Trial-expiry UX — messages / lockout / re-enable without losing settings (owner Qs, parked) | **Parked 2026-06-22 — answer next (owner: "circle back" after auth promote, now done)** | Owner questions: (1) what happens when a user passes the 14-day trial? (2) what messages do they see? (3) are they locked out? (4) how to re-enable WITHOUT losing settings completed during the trial? **Initial code read — VERIFY before giving a definitive answer:** access gate `showApp` (index.html ~6698) grants when `status ∈ active/trialing/tester/pro/enterprise`; trial enforcement is gated by **`ENFORCE_TRIAL` (appears FALSE today → expiry would lock NOBODY out currently)**; `trialState()` recomputes from `trial_ends_at`; `#paywallScreen` already has copy for trial-ended / past_due / canceled. Re-enable = super-admin subscription screen (`crewlogic-admin` `setSubscription`: make_permanent / extend / reactivate) flips status. **Settings are NOT deleted on expiry** — only access is gated, data persists, so re-enabling restores everything. Confirm against code + prod before answering. |
| One-tap text the **customer** (SMS pill by phone #) | **Done — Live in prod (promoted v5.39.1, 2026-06-14)** | Owner clarified 2026-06-13: this texts the **customer**, not the crew. Added a **💬 Text** pill (`#estSmsDisplay`, `sms:<digits>`) next to the existing customer tap-to-call (`#estPhoneDisplay`) on the estimate-editor contact row — same number, opens the device messaging app. Frontend-only (markup ~2111 + phone-render block ~15879). Could later add SMS next to phone numbers elsewhere (job/route briefs) if wanted. |
| Google Maps keys: authorize `app.crewlogicai.com` referrer (two-URL-split fallout) | **✅ DONE — yard-signs + Street View both working in prod (owner added `https://app.crewlogicai.com/*` to the `…fhCgM` Street View key; prod-tested 2026-06-15)** | Was: `…fhCgM` Street View key allowed `crewlogicai.com` (apex) + `dev` but NOT `app.crewlogicai.com` → Street View `fetch()` 403'd → "No Street View"/logo fallback. Code (`fetchStreetViewPhoto` index.html:13994) was correct/untouched; pure key-config fix. **Durable hardening moved to the credential-cleanup project** (proxy Street View server-side so a domain change can't break it again). | Yard-signs map failed with **`RefererNotAllowedMapError`** on app.crewlogicai.com. The Maps JS key (`SIGNS_MAPS_KEY`, ends `…YBI1g8`, index.html:19654) has HTTP-referrer restrictions that predate the two-URL split and don't include the **app** subdomain. **Fix (Google Cloud Console → APIs & Services → Credentials → that key → Application restrictions → HTTP referrers):** add `https://app.crewlogicai.com/*` + `https://dev.crewlogic.pages.dev/*` (keep existing `https://crewlogicai.com/*`). **Also** `STREET_VIEW_KEY` (ends `…gfhCgM`, index.html:4620) — same gap (Street View on estimates won't load on the app subdomain). Config-only, no code change; propagates in a few minutes. **Key→project map (Google Cloud):** `…YBI1g8` lives in project **CrewLogic Maps**; `…fhCgM` lives in project **CrewLogic StreetView**; old/unused keys (incl. a `…dlpuY` seen earlier) live in **Nano Banana Test** (ignore). Keys can't be moved between projects, so fixed in-place. Resolved by owner 2026-06-15 (referrers added to both). |
| **Google Cloud credentials cleanup / rationalization** | **Not started — project; plan before executing** (2026-06-15) | Owner goal: **kill every unused Google key/credential and consolidate all CrewLogic Google credentials into ONE rationalized project** (single account). Current sprawl (discovered 2026-06-15 fixing the yard-signs referrer): keys live across multiple projects + two Google logins — `…YBI1g8` (Maps JS / signs) in **CrewLogic Maps**; `…fhCgM` (Street View) in **CrewLogic StreetView**; stale keys (incl. `…dlpuY`) in **Nano Banana Test**; the **OAuth sign-in client** ("CrewLogicAI Web Client") under the **personal** Google login; older "My First Project". **Important:** Google API keys **cannot be moved** between projects — rationalizing = **create new keys in the target project → update code/secrets → delete old keys → decommission stale projects**. **Plan steps (when picked up):** (1) **Inventory** all projects/keys/OAuth clients/service accounts across BOTH logins — incl. **server-side** Google usage: `crewlogic-estimate` `calcDistances` (Google Distance Matrix) + any other edge-fn keys / Supabase secrets (note: job geocoding uses FREE US-Census, not Google). (2) Pick the **one target project** (+ which login owns it). (3) Create new keys with correct **API restrictions** + **Application restrictions** — client Maps key(s) referrer-restricted to app/crewlogicai/dev; server key(s) **IP-restricted, not referrer** (server has no referrer). (4) Update `index.html` `SIGNS_MAPS_KEY`/`STREET_VIEW_KEY` + any prod Supabase Google secret (secret-set is gated). (5) Verify on dev: yard-signs map, estimate Street View, Route-Optimizer/cost-analysis distance calc — then promote. (6) Delete old keys + decommission **Nano Banana Test** / **CrewLogic Maps** / **CrewLogic StreetView** / **My First Project** once migrated. (7) Update the `google-maps-keys-projects` memory + this row. **Owner does Console actions; Claude does code + (gated) secret/deploy.** **INVENTORY DONE 2026-06-15** — every Google credential the app uses: **Client (index.html, browser, referrer-restricted):** (1) Maps JS key `…YBI1g8` = yard-signs map (`SIGNS_MAPS_KEY` :19654, loader :19664) → proj **CrewLogic Maps**; (2) Street View key `…fhCgM` = estimate Street View (`STREET_VIEW_KEY` :4620, img :13803) → proj **CrewLogic StreetView**; (3) OAuth Client ID `183516161425-…apps.googleusercontent.com` = Google Sign-In (:40, :3614) → OAuth client **"CrewLogicAI Web Client"** (personal login). **Server (Supabase Edge secrets):** (4) `GOOGLE_CLIENT_ID`+`GOOGLE_CLIENT_SECRET` = OAuth token exchange (`crewlogic-oauth-callback`), set prod **+** dev → same OAuth client as #3; (5) `GOOGLE_GEOCODING_API_KEY` = Geocoding (`crewlogic-ai` reverseGeocode) **+** Distance Matrix (`crewlogic-estimate` calcDistances, via fallback), set **prod only**, no-referrer server key → **unknown project (owner to locate)**. **Findings:** `GOOGLE_MAPS_API_KEY` referenced in code but **never set** (prod or dev) → Distance Matrix rides the geocoding-key fallback, so that key needs **both** Geocoding+Distance Matrix enabled; **dev has NO geocoding key** → cost-analysis distances + AI reverse-geocode error on dev (prod-only) — set the server key on dev during cleanup. Not creds: `lh3.googleusercontent.com` (legacy Drive photos), Google Fonts. **APIs to enable in target project:** Maps JavaScript · Street View Static · Geocoding · Distance Matrix. **Recommended end-state (1 project, personal login):** ONE browser key (Maps JS + Street View Static, referrer-restricted) replacing `…YBI1g8`+`…fhCgM`; ONE server key (Geocoding + Distance Matrix, no referrer, server-only) as `GOOGLE_GEOCODING_API_KEY` (+`GOOGLE_MAPS_API_KEY`) on prod **and** dev; keep the OAuth client. **Next action (owner):** (a) locate which project holds `GOOGLE_GEOCODING_API_KEY`; (b) pick the single target project; then Claude creates the 2 keys + repoints code/secrets on dev → verify → promote → delete old keys + decommission CrewLogic Maps / CrewLogic StreetView / Nano Banana Test. **ADDED 2026-06-15 (owner) — durable Street View hardening:** as part of cleanup, **proxy the Street View Static fetch through an edge fn** using the no-referrer server key (instead of the browser calling the referrer-restricted `…fhCgM` key directly). Same pattern fits any other referrer-fragile browser→Google call. Eliminates the two-URL-split-style 403s for good (Street View 403'd on app.crewlogicai.com 2026-06-15 for exactly this reason). `fetchStreetViewPhoto` (index.html:13994) would call the edge fn instead of `maps.googleapis.com/maps/api/streetview` directly. |
| Reorderable home-screen cards (per-user preference) | **🚀 LIVE IN PROD (v5.41.0, 2026-06-16)** · owner chose (a) DRAG + (b) DB · migration 0021 + crewlogic-settings deployed to prod, dev→main merged | Owner request: let users reorder the home feature cards to their preference. The 7 `.module-card`s live in `.module-grid` (index.html:1269-1312), each `onclick="openModule('X')"`; some are role/config-gated (`routerCard`/`trucksCard`/`signsModuleCard` default `display:none`). **Approach:** persist a per-user ordered list of card keys (derive key from the `openModule` arg: estimates/volumeCheck/router/trucks/priceLookup/signs/jobPlan); on home render, order the visible cards by it and append any not-yet-ordered card at the end (so new features still surface); gating stays independent. **Open decisions:** (a) interaction — **edit-mode ↑/↓** (recommended; touch-reliable, no new dep on this mobile-first PWA) vs drag-and-drop (nicer but native HTML5 DnD is poor on touch → custom pointer-events, more edge cases); (b) persistence — **localStorage per-device** (fast v1) vs **DB `profiles.home_card_order`** (cross-device, +migration + save/load). Lean: ↑/↓ + localStorage for v1, DB/drag as upgrades. **Optional extension:** also let users hide unused cards. Frontend-only (small) unless DB. Build dev-first after owner picks (a)+(b). **BUILT ON DEV (v5.41.0, 2026-06-16):** (1) migration `0021_home_card_order.sql` (`profiles.home_card_order jsonb`, applied to dev); (2) `crewlogic-settings` new **caller-verified** `saveHomeCardOrder` action (getUser(token) → updates ONLY `home_card_order` on the caller's own row by `auth_user_id`; payload validated; deployed dev); (3) frontend: **SortableJS 1.15.6** (CDN — new dep, YELLOW) on `.module-grid` with `delay:200`+`delayOnTouchOnly` so tap still navigates / long-press drags on touch; `data-card` keys on all 7 cards; `applyHomeCardOrder()` on `goHome()` + on the cached-session background-refresh (cross-device sync); `home_card_order` loaded into `currentUser`. Gating untouched. **Optional follow-up:** let users hide unused cards. **PROMOTE (gated):** migration 0021 → prod; deploy `crewlogic-settings` → prod; merge dev→main. |
| **Payments + pricing model** (top priority) | **Strategy / planning (started 2026-06-16)** | Owner: get the app ready to **accept card transactions** + design a **pricing model**. **Decisions so far (owner 2026-06-16):** (1) **billing unit = franchise location** (matches the schema's billing entity); (2) model = **matrix of seats × feature-gates + a per-tier AI-estimate allowance** (the allowance ties price to the #1 marginal cost); (3) **no known market comp → price against cost-to-serve** (value-based). **Foundation already built:** subscription schema (`subscription_status`/`subscription_tier`: trialing/tester/active/pro/enterprise/canceled), per-franchise trial clock (migrations 0016/0017), the super-admin subscription screen, the access gate. **Stripe is greenfield** (no payment code yet — grep confirmed). **Plumbing plan:** Stripe Checkout (trial→paid from the existing paywall) + **webhook→edge fn** that flips `subscription_status`/`subscription_tier` on pay/cancel/fail + Customer Portal (self-serve card/cancel) + `franchises.stripe_customer_id` + **DB/Stripe-stored price IDs (never in code** — per pricing-never-in-code rule). **Cost drivers ranked (qualitative):** 1) **Anthropic AI vision calls** (per estimate × photos — dominant variable cost) > 2) Google Maps (Street View/Distance Matrix/Geocoding per estimate; job-map geocoding is FREE US-Census) > 3) Supabase Storage+egress (photos, grows; free tier today) > 4) Supabase compute/edge invocations > 5) Resend/n8n (low/fixed); telematics = franchise's own cost, not CrewLogic's. **In progress:** cost-to-serve analysis (per-action paid-call map) → then tier matrix → **plan doc for owner approval** → build dev-first. **PREREQUISITE BUILT: usage metering (see next row).** **PLAN DOC DRAFTED 2026-06-18: `docs/plan-payments.md`** (awaiting owner approval) — locks 3 tiers (Starter/Pro/Enterprise) · gate advanced features by tier · per-tier AI allowance · soft-cap+upgrade-nudge overage (v1) · Route Optimizer = roadmap (not launch) · Live Trucks stays Pro · Stripe Checkout+webhook+Portal plumbing · field-model finishers folded in. |
| Usage event metering (cost / billing backbone) | **🚀 LIVE IN PROD (v5.42.1, 2026-06-16)** — migration 0022 applied to prod; crewlogic-ai/-estimate/-job-plan deployed to prod; dev→main merged; smoke-verified. Real per-franchise usage now logging in prod. | Append-only `usage_events` (migration `0022`, RLS service-role-only, applied to dev) + `_shared/usage.ts` **non-throwing** `logUsage` (UUID-coercing, fire-and-forget — a log failure can never break a request). Metered server-side in **crewlogic-ai** (analyze vs **volume_check** split via `source`, classify, job_summary, detect_sign, geocode), **crewlogic-estimate** (distance_matrix + element count), **crewlogic-job-plan** (Haiku) — capturing **ACTUAL Anthropic input/output tokens** + image/element counts, attributed to franchise+tenant. Frontend passes `franchiseID`/`tenantID` + `source` on crewlogic-ai calls. **VERIFIED ON DEV:** a real analyzeEstimate logged `ai.analyze_estimate`/sonnet-4-6/tokens_in 2344/tokens_out 176 (text-only → confirms **~2.3K system-prompt baseline + ~1.5K/photo** cost model). **Deferred:** Street View metering (needs the server-proxy), price.lookup/estimate.created events, **super-admin usage/cost dashboard**. **PROMOTE (gated):** migration 0022 → prod; deploy `crewlogic-ai`/`-estimate`/`-job-plan` → prod; dev→main. This is the enforcement source for tier AI allowances + overage billing. |
| Price-lookup: friendly out-of-territory error | **🚀 LIVE IN PROD (2026-06-16)** — deployed to prod + smoke-verified (02368→friendly, 02360→price list). Also v5.42.1: job-lookup status now on its own full-width wrapping line so the message shows fully (was truncating). | `crewlogic-price-lookup` was leaking the raw Vonigo error (`priceLists method 2 failed: Data validation failed`) to the user when a ZIP isn't in the franchise's Vonigo zone — happens on **courtesy / out-of-area jobs** (job on our schedule but tagged to another franchise's territory; e.g. job 853450 / Randolph 02368 for #90). Now returns `No price list for this ZIP (outside your territory).` (code `ZIP_OUT_OF_TERRITORY`); full error still `console.error`'d server-side, other errors return a safe generic message (no-internals-to-client). **NB Vonigo users can't price manually (native-only), so this is just a clearer message, not a fallback.** Verified on dev (02368→friendly; 02360 in-territory→price list intact). Edge-fn only (no version bump). **Promote (gated):** deploy `crewlogic-price-lookup` → prod (rides with the metering bundle / next dev→main). |
| Auth trap: "Name your workspace" had no clear sign-out | **🚀 LIVE IN PROD (v5.43.5, 2026-06-17)** | Owner hit on mobile (prod): a half-provisioned account (`tpass2008` — Supabase login but no completed workspace) routes to the post-signup `showNameWorkspaceScreen` ("Name your workspace"), whose only escape was a button **labeled "Cancel"** that actually calls `signOut()`. Looked like a dismiss, not an account switch → owner felt trapped, couldn't reach their Junkluggers Google account. **Fix (index.html:6269):** relabeled to **"Sign out / use a different account"** + bordered button style; same `signOut()` action. Frontend-only. (Password-reset screen's "Cancel" left as-is — different context.) **Follow-up (low pri):** investigate whether a *fully provisioned* account can ever mis-route to this screen (profile-lookup edge case); owner's real Google account is unaffected. **Promote:** ships with the dashboard bundle (dev→main). |
| National Accounts: auto-fill job Summary w/ simplified crew warning | **Research done; trigger DECIDED (2026-06-23) = nightly cron — build not started.** Enterprise-tier / JL-only feature (see `docs/plan-payments.md` §3). | Goal: take the verbose customer "warning message", simplify it (AI prompt, ≤~50 words, on-job actions only), and write it to the job's **Summary** field for National Accounts jobs. **Vonigo API findings (proven on job 853608):** (1) the crew **warning message** is on the **Client/customer** object, **field 9059** (HTML) — get via job → `client` relation objectID → `POST /data/Clients/ {clientID}`. (2) The **job Summary** is **field 978 on the Job object** — get via `POST /data/Jobs/ { objectID: <jobID> }`, **response key is `Job` (singular), NOT `Jobs`**. (3) **WRITE works**: `POST /data/Jobs/ { method:"2" (edit), objectID, Fields:[{fieldID:978, fieldValue}] }` → errNo 0, read-back confirmed. (4) Service-type filter: Job field 982 / `servicetype` relation objectID 27 = "National Accounts". **Gotchas:** Vonigo **URL-decodes field values → eats `+` (and likely `&`)**, replacing with a space — use "and"/commas in content. Newlines `\n`, emoji (⚠️ ✅), `→ · —` all preserved + render; **but** Vonigo's collapsed "More…" preview flattens line breaks (its UI — crew taps More for the checklist). Owner left the ✅ checklist live on job 853608's Summary as the example. **Efficiency note:** warning is per-customer, summary per-job → simplify once per customer (cache), stamp onto each NA job. **Trigger DECIDED (owner 2026-06-23): nightly cron** — sweep the *next day's* National-Accounts bookings (service-type 27) and stamp the slimmed message onto each job's Summary, so the crew reads it in Vonigo the morning of. (Cadence matches the UX promise: written the night before, read at job time.) Build NOT started. **Prompt rule-set so far (the simplify prompt taking shape):** (1) on-the-job actions only (drop pre-job SOP + post-job invoicing/CC/billing admin); (2) **≤60 words** (raised from 50 to fit the dry-run fee); (3) `⚠️` header w/ account name, then `✅` per action line; (4) strip "I understand…" → terse actions; (5) keep exact identifiers verbatim — phone, email, NTE, **and the dry-run fee amount IF the warning states one** (SLM has none→omit; Rubicon $200 / Reloc Remedies $150→include); (6) **no `+`/`&`** (Vonigo eats them) — use "and"/commas. |
| Super-admin usage/cost dashboard | **🚀 LIVE IN PROD (v5.43.5, 2026-06-17)** (incl. storage panel + by-tenant breakdown/filter) — `crewlogic-admin` deployed to prod, dev→main merged, smoke-verified (gated 403). **NB:** `STORAGE_INCLUDED_GB` not set on prod → defaults to **1 GB** (current free tier); **set the prod secret to 100 when on Supabase Pro**. |
| UX/bug fixes (v5.43.3–.5) | **🚀 LIVE IN PROD (v5.43.5, 2026-06-17)** | (1) **Animated spinner on all AI-analysis wait states** (estimate photo analyze, per-charge + "Analyze All" batch, re-analyze, voice/describe, Street View classify, yard-sign read, Volume Check) — replaced static `⏳`/`🤖` so they don't look frozen; non-AI waits left alone. (2) **False "photo not synced" warning fix** — `confirmIfUnsyncedPhotos` now runs `reconcileUploadedPhotoRefs()` first: a `photo_` ref whose IDB record is `uploaded`+`supabasePath` is swapped to the Storage path (and self-healed), so already-synced photos no longer false-warn on Vonigo submit (root cause: `_uploadPhotoFromQueue` ref-swap is gated on `currentEstimate.estimateID` matching at upload time, index.html ~9181; a reloaded estimate left a stale local ref). All frontend-only. | Reads the live `usage_events` meter. **Backend:** `crewlogic-admin` new `usageSummary` action (runs after the existing super-admin caller-verify; service-role query of `usage_events` over a 7/30/90-day window; applies OUR Anthropic/Google cost rates per row — sonnet $3/$15, haiku $1/$5, maps per-call — clearly marked as cost-not-customer-pricing; aggregates **byType** (event_type+model: events/tokens/cost) and **byFranchise** (name-joined; null→"Unattributed"); 50k cap + flag). **Frontend:** super-admin `#usageDashCard` (Settings) → `#usageDashboardScreen` with window selector + summary + by-feature + by-franchise tables; gated to `charles.dennis@junkluggers.com`. **Verified:** non-super-admin caller → 403. Real numbers only show once promoted (prod is where usage logs). **Promote (gated):** deploy `crewlogic-admin` → prod; dev→main. **Next after data accrues (~3-5 days):** pull real cost-to-serve → finalize tier matrix + pricing plan doc. |
| Trucks map: gray look-ahead dots for jobs in the next 3 days | **Live in prod (v5.39.1, 2026-06-14) — owner-tested on dev** | On "Where Are My Trucks?", show **small gray dots** for any job in the **next 3 days relative to the selected Jobs day** (in addition to the colored route-numbered markers for the selected day). Window = selectedDay+1 … selectedDay+3: **Today selected → dots for the next 3 days** (e.g. today=Jun 14 → dots Jun 15–17); **Tomorrow selected → dots Jun 16–18**. Dots are **hoverable** with context (client / time / route / address). Add a **show/hide toggle button** on the map for the look-ahead dots. Data: reuse `crewlogic-todays-workorders` with `dayOffset` for the +1..+3 offsets (includeCoords). Frontend-only on top of the existing jobs overlay. Use case: see what's coming so a nearby truck can pull a job forward. |
| Graceful stale-session handling (no log-out/in "shuffle") | **Core Live in prod (v5.39.1, 2026-06-14)** — `buildSessionFromSupabaseAuth` now silently `refreshSession()`s + re-reads ONCE when the profile loads but the franchise embed doesn't (stale access token → anon → RLS blocked the embed), then **complete-or-fail** (returns null rather than a half-built currentUser). A stale-but-refreshable session (access token expired, refresh token valid — the common case, incl. the 2.5-day dev session) now recovers silently = no bounce. **Deferred:** a soft "Reconnecting…" banner for the rare *truly-dead* session (refresh token expired/revoked) — that still routes to a clean login. | Owner: users must NOT be bounced (sign out → sign in) when their app session goes stale. **Root-cause arc (2026-06-14):** an expired Supabase session (gotrue "expires in -214317s", 403 on `/auth/v1/user`) → on reload `buildSessionFromSupabaseAuth` left `currentUser` **partial** — trucks worked (via `franchiseInternalID`) but the jobs call sent a bad `franchiseID` → `crewlogic-todays-workorders` 404. NB the data calls use the **public app key** + the franchise's Vonigo creds (re-auth'd every call), so they should NOT depend on the login session; the breakage came from a half-built currentUser, not the key. **Desired:** (1) **silent session auto-refresh** (never user-visible); (2) **complete-or-fail `currentUser` rebuild** — never leave franchiseID/franchiseInternalID inconsistent; (3) data calls **robust to session state**; (4) worst case a subtle inline **"Reconnecting…"** + one-tap re-auth — never a hard logout that loses the user's place. Affects real users who leave the app open (esp. Google-OAuth owners — ties to the deferred unified-auth work). |
| "Where are my trucks?" — live truck locations (Linxup or Motive) | **🚀 LIVE IN PROD (v5.36.1, 2026-06-12)** — Phase 1 data layer DONE on dev (2026-06-12): `crewlogic-trucks` extended to support **Linxup** (pull `GET /ibis/rest/api/v2/locations`, `Authorization: Bearer <LINXUP_API_KEY>` — secret holds raw token) **+ Motive** (default), normalized to `trucks[]`; deployed + verified on dev (3 trucks, owner's Linxup acct). `LINXUP_API_KEY` set on prod + dev. **Phase 1 COMPLETE on dev (v5.31.0, 2026-06-12):** home tile "Where Are My Trucks?" (gated to #90 owner) + a Leaflet map + truck list reading the live Linxup feed — Playwright-verified (3 trucks, map markers, no errors). Leaflet 1.9.4 added to `<head>` (new dep; free Carto/OSM tiles, no key). **Phase 2 BUILT on dev (v5.32.0, 2026-06-12):** per-franchise telematics. Migration `0018_telematics_credentials.sql` (`telematics_credentials` + `_audit` tables, Vault RPCs `upsert/get/get_status/set_status`, service-role-only grants) applied to dev. `crewlogic-trucks` now resolves the franchise's provider+token from Vault per request via `?franchiseID=` (legacy global-env `?provider=` fallback kept — Route Optimizer at index.html ~7415 still works). `crewlogic-settings` gains `saveTelematics` (token→Vault, live-validate, stamp status) + `getTelematics` (non-secret status, no token). Shared `_shared/telematics.ts` does the Motive(`x-api-key`)/Linxup(`Bearer`) fetch+normalize for both serve + validate. New **owner-only Settings 🚚 Trucks tab**: provider flag (Motive\|Linxup) → flag-driven setup steps → token input → "Save & Test" → "Connected ✓ — N trucks". Home tile now shows for **all owners** (was #90-only); screen reads the franchise provider; not-configured → CTA to Settings. Backend smoke-tested on dev (unconfigured→404, getTelematics→configured:false, legacy linxup fallback→3 trucks). **UI polish + jobs-on-map (v5.33.0, 2026-06-12, on dev):** numbered truck markers (1/2/3) matching numbered list rows; map enlarged (60vh); rich Motive popups (year/make/model, relative location, speed/heading, status, VIN — confirmed via live #90 pull what Motive v1 returns). Today's **open** jobs overlaid as 🏠 markers + legend; addresses geocoded via the FREE **US Census** geocoder (no Google, no key) cached in `geocode_cache` (migration `0019_geocode_cache.sql`, dev); archived/completed jobs hidden (`isComplete`). `crewlogic-todays-workorders` gained `includeCoords`. Verified on dev: 2 open jobs geocoded, 5 archived skipped, cache 2/2. **Increments v5.33.1–v5.35.0 (2026-06-12, on dev):** (a) **auto-refresh** every 45s while the screen is open (silent, no map re-fit; jobs load once) — diagnosed the "truck in wrong spot" report as **Motive-side staleness, not an app bug**: the app calls Motive **live on every Refresh** (no caching), verified by consecutive pulls (moving trucks ~90s fresh, parked trucks hours old); (b) **job markers color-coded by Vonigo `route` + numbered by stop** (1/2/3 within each route, time-sorted; `route` already in the workorders response — frontend-only; rounded squares vs round truck markers; per-route legend chips); (c) **Today/Tomorrow toggle** for the jobs overlay (uses `dayOffset`; trucks stay live) — owner use case: spot a truck near a tomorrow stop today to pull it forward. Verified tomorrow returns 5 open jobs across 2 routes, all geocoded. **OPEN FOLLOW-UP — deferred (owner decision 2026-06-12):** harden `crewlogic-trucks` caller-verification — **folded into the future unified-auth work** because Google-OAuth owners have no Supabase JWT to verify per-request; residual risk low (attacker needs a victim's random internal franchise UUID + the public anon key). Owner/next-action: revisit when Google logins get a Supabase session. **PROMOTED TO PROD 2026-06-12 (v5.36.1):** migrations 0018+0019 applied to prod (3 tables + 4 RPCs verified), 3 edge fns deployed to prod (`crewlogic-trucks`/`crewlogic-settings`/`crewlogic-todays-workorders` + `_shared/telematics.ts`), `dev`→`main` ff-merge pushed → Cloudflare built index.html v5.36.1 to app.crewlogicai.com. Prod backend smoke-verified (trucks→"not configured", getTelematics→configured:false; no writes). **Remaining owner steps:** re-enter the Motive token in **prod** Settings → 🚚 Trucks (per-franchise Vault — doesn't carry from dev), then validate trucks + jobs on prod. **Feature uses v2 Motive feed + FREE US-Census geocoding (no Google/no cost).** Spec: `docs/plan-telematics-per-franchise.md`. **UX BATCH v5.45.8–.16 → PROD 2026-06-19 (frontend-only, no migrations/edge-fn changes):** smaller truck circles + **numbered house icons** for jobs; route legend shows **Vonigo codes** (MA1REG/MA2FAR, extracted from the route field's parens); job popups — time-in-header, **proper-case address**, **clickable formatted phone** (860-387-8366; lazily prefetched on map load via `crewlogic-job-lookup`), **nearest-named-truck distance** (Motive truck name + haversine mi); **green** 3-day look-ahead dots (now click-popups) with truck distance + Vonigo link; **satellite/street toggle** (Esri imagery, no key); **"Open job in Vonigo"** link. NB: Vonigo APPOINTMENT deep-link (`Job_WorkOrder.aspx?woID=`) tested + **reverted** — it only renders inside Vonigo's own frame; link goes to the **Job page** + 1 click. In-app reschedule logged as a future build (see Backlog). Also on dev/main but NOT prod-deployed: `crewlogic-geofence-create` edge fn (geofence test/R2 foundation). | New **main feature on the home screen** showing current truck/GPS locations. Build supporting **Settings pages** so an owner enters their telematics API credentials for **Linxup** OR **Motive** (per-franchise; choose provider). Owner has accounts with **both** → test with their credentials. NB: a `crewlogic-trucks` edge fn + `MOTIVE_API_KEY` secret already exist (Route Optimizer truck-distance display, Motive/gomotive) — generalize/extend that for the home feature; **Linxup is net-new**. Store creds **server-side (Vault, like Vonigo)**, not plaintext. Add a home tile + a truck-map/list view. |
| Vonigo date-range pull → CRM pipeline (leads / estimates / cancellations / cases / UCBs) | **Not started — bigger project** (2026-06-11) | Date-range pull from Vonigo of **leads, estimates, cancellations, cases, and UCBs**, managed inside CrewLogicAI with **CRM-pipeline-style functionality** (stages, status, assignment, follow-ups). Goal: **stop things falling through the cracks** — unbooked leads, cancellations to re-engage, open cases. Bigger build: a Vonigo data-pull (new edge fn or extend existing Vonigo calls — MD5 `/security/login/` auth, no OAuth), a storage/sync model, and a pipeline UI. **Open questions:** define "UCB"; map each category to its Vonigo endpoint/object; pull cadence (on-demand date range vs. scheduled sync). |
| Shed / building estimator undercounts volume (~½) | **Not started — follow-up project** (2026-06-12) | Owner-reported: the shed/building volume estimator calculates **about half** the actual volume (~2× low). Likely root: missing structural inputs that drive the volume math. **Proposed new inputs:** (1) **floor joist depth** — 6″ / 8″ / 10″; (2) **roof joist depth** — same 6″ / 8″ / 10″ options; (3) **number of shingle layers** on the building. Scope for the follow-up: locate the shed/building volume calc (the `calc*` engine in `index.html`), determine why output is ~½, then add the three inputs to the estimator UI + fold them into the volume calculation. Verify against a known shed where the current estimate is half-right. |
| Voice command → move a job between routes (AI interprets → Vonigo write) | **Not started — future project** (2026-06-12) | Owner request: issue a **voice command** to move a job from one route to another → **AI interprets** the intent → **connects to Vonigo** and alters the job so it appears on a **different route / time / day**. Scope: (1) voice capture + transcription (possibly reuse the existing Describe/voice path); (2) an AI intent-parser (identify which job + the target route/time/day); (3) a **Vonigo write** to reschedule/reassign the job (MD5 `/security/login/` auth — note `vonigo-pricing-read-only-api` research found Vonigo writes ARE possible via Edit params). Naturally pairs with the Vonigo CRM-pipeline project. **Open questions:** which Vonigo endpoint/object updates a job's route/schedule; how routes are represented in Vonigo; voice→intent disambiguation (which job, which route) + a confirm step before the write. |
| Email + password auth (native, non-Google) | **Done — Live in prod** (v5.30.0; re-verified in prod v5.30.3, 2026-06-11) | Replaces magic-link-only friction (see roadmap memory). **Phase A — app login:** `loginV2` email path is now password-first (`signInWithPassword`) + **forgot-password** (`resetPasswordForEmail`) + magic-link fallback; junkluggers emails still route to Google. **Phase B — reset landing:** `onAuthStateChange(PASSWORD_RECOVERY)` + URL-hash fallback in `initAuth` → "Set a new password" screen (`updateUser`). **Phase C — signup with password:** app-hosted "Create your account" screen runs `signUp` on the APP origin (session lands in-app, no inbox detour) → provisions via `crewlogic-signup`; marketing form now hands lead info to `app.crewlogicai.com/?signup=1&…` instead of a magic link; "Start a free trial" link on app login. Email confirmation expected OFF (instant session). **Files:** `index.html` + `marketing/index.html`. NB: `crewlogic-signup` must be deployed to **dev** to exercise Phase C there (prod-only today). |
| Provisioning/access matrix — **2 open items** (build complete) | **Built + LIVE in prod (all 6 cells, v5.30.3)** — remaining = (1) prod QA + (2) owner DNS | The full grid (entry point × email domain → Vonigo/native + clock) is coded & deployed; both Google and email+password paths live. Canonical: `docs/provisioning-access-matrix-spec.md` + `docs/onboarding-playbook.md`. **Open #1 — live Vonigo-connect prod validation (QA, not code):** the two `@junkluggers.com` cells (invite + marketing) were never run end-to-end in prod (`crewlogic-settings` hardcodes the prod Junkluggers tenant → couldn't dev-test). Validate on a real onboarding: `@junkluggers.com` user → Vonigo-pending profile → Settings → enter Vonigo UID+pw → franchise attaches under Junkluggers tenant, **no paywall**, and (marketing cell) the 14-day clock carries onto the franchise. Best done when onboarding a real franchisee (e.g. Ben Spilger). **Open #2 — confirm-email deliverability: DMARC NOW ADDED ✅ (verified 2026-06-15** via `dig _dmarc.crewlogicai.com` = `v=DMARC1; p=none; rua=mailto:dmarc@crewlogicai.com; fo=1`; SPF/DKIM already pass). Earlier the "Confirm signup" email delivered but hit **junk** (root cause = missing DMARC). Now DMARC is in place. **Remaining (minor):** do a real signup re-test to confirm the confirm-email now lands in the **inbox**; optional polish = move sender off `noreply@`; custom Supabase auth domain (so the verify link matches crewlogicai.com) stays deferred (paid). |
| Super-admin subscription/trial management screen | **🚀 LIVE IN PROD (v5.41.0, 2026-06-16)** — migrations 0020 applied to prod; `crewlogic-admin` + `crewlogic-oauth-callback` deployed to prod; dev→main merged. Access-gate change live (tester-safety pre-verified: 0 prod rows affected). | Owner wants a super-admin screen (gated to `charles.dennis@junkluggers.com`) to manage account clocks with **3 actions**: (1) **make permanent / never-expire** (`subscription_status='tester'`, `trial_ends_at=NULL`); (2) **extend trial** (push `trial_ends_at` out — +7/+14/+30 or date picker); (3) **set end date / end now** (set `trial_ends_at`; today/past = immediate paywall). All three reduce to setting (`subscription_status`, `trial_ends_at`). **Key design point:** write at the **franchise** level uniformly — the access gate reads franchise-first (index.html:4426), so this is safe for the shared Junkluggers `tester` tenant (NEVER write the shared tenant or you change the clock for all JL franchises at once). Native clock currently lives on the tenant, but a franchise-level override wins, so franchise-write is correct + uniform. **Backend:** super-admin-gated edge action that **re-verifies caller server-side** (JWT email claim, not just client gate) → service-role PATCH of the franchise row → **audit row** (like `vonigo_credential_audit`). Not direct client writes (RLS blocks; don't widen). **UI:** list accounts (email · track native/Vonigo · status · days-left) + per-row actions, in the existing super-admin area. Build dev-first after design approval. **Open decisions (owner):** (a) full account list vs search-by-email for v1 (lean: list); (b) extend from-today vs from-current-end (lean: from-today + date picker); (c) hard-cut = past date vs distinct `canceled` status (lean: past date for v1). **DECISIONS (owner 2026-06-15):** (a) **searchable** by last/first/email — build now; (b) extend **from today**; (c) include **`canceled`** status (needed for future automation). **BUILT ON DEV (v5.40.0, 2026-06-15):** (1) migration `0020_subscription_audit.sql` (applied to dev — `subscription_audit` table, RLS on, service-role-only); (2) new edge fn **`crewlogic-admin`** (deployed dev, `--no-verify-jwt`; verifies caller JWT email === super-admin via `getUser(token)` BEFORE any action, then service-role) with `searchAccounts` (name/email ILIKE, returns effectiveStatus/effectiveTrial) + `setSubscription` (ops: make_permanent/extend/set_end_date/cancel/reactivate; writes franchise-level; audits every change); (3) frontend: `adminEdgeCall` (sends user JWT), super-admin Settings card `#adminSubsCard` + `#subscriptionAdminScreen` (search box + per-account action buttons), gated to `charles.dennis@junkluggers.com`. **GATE CHANGE (sensitive):** the access-gate IIFE now treats `franchise.subscription_status` as **authoritative when set** (can REVOKE, not just grant) in BOTH `buildSessionFromSupabaseAuth` (index.html) AND `crewlogic-oauth-callback` (deployed dev) — the `subscription_tier='free'` trap fix preserved. **Tester-safety verified:** read-only prod check returned **0 franchises with a non-null `subscription_status`**, so the gate change affects zero existing accounts (all fall through unchanged). **v5.40.1 refinement (owner req):** the screen now **lists ALL accounts on open + live client-side filter** (name/email/franchise), mirroring the estimate customer-lookup UX — `searchAccounts` query is now optional (empty → returns all, order by name, limit 200); frontend `loadSubscriptionAccounts()`/`filterSubscriptionAccounts()`. **v5.40.2:** status pill lightened (`#94a3b4`) for readability. **v5.40.3:** status pill reads `tester · permanent` / `trialing · expires <Month D, YYYY>` (via `_fmtDate`); **Set end date** now uses a native `<input type=date>` calendar picker (no free-text format); edge fn pins a date-only pick to **noon UTC** to avoid the off-by-one. **v5.40.4:** replaced the inline date field with a **"Select expiration date" button** that opens the native calendar via `input.showPicker()` (hidden date input; `_subPickDate`). **PROMOTE STEPS (gated):** apply migration 0020 to prod; deploy `crewlogic-admin` + `crewlogic-oauth-callback` to prod; merge dev→main. |
| 🔴 Native estimate toolbar dead on reopen (undefined `renderChargeRows()`) | **Fixed + owner-verified on dev (2026-06-11); pending prod promote** | On reopening a native draft, `autoRefetchPriceBlocks` (index.html ~8753) loads pricing then called **`renderChargeRows()` — a function that doesn't exist** (should be `renderCharges()`, the real fn at 10956). The ReferenceError threw BEFORE the very next line `updateEditorToolbarState()` (8784), so the toolbar (Photo/Describe/Manual/More/Save Draft — all `.est-add-btn`, gated on `priceBlocks.length` for native) stayed disabled **even though pricing loaded**. The NEW-estimate path (`loadEstimatePricing`) calls the correct `renderCharges()` — that's why only reopen broke. **Confirmed via live console** (`ctdennis@iwavesolutions.com` / `native-cde571a5`, est `1781213846408`): `pricingSource:'native'`, `priceBlocksLen:4`, `btnDisabled:true` — pricing loaded, toolbar dead. **Fix:** `renderChargeRows()` → `renderCharges()` (index.html:8786) — deployed v5.30.4, **owner-verified on dev 2026-06-11** (hard-refresh → reopen native estimate → toolbar auto-enables). **Delay polish (v5.30.5):** the reopen pricing-refetch had a hardcoded `setTimeout(...,300)` (index.html:8760) → changed to `0`, so the toolbar enables as soon as the lookup returns instead of 300ms later. (Note: normally-saved estimates persist `priceBlocks` and take the instant path with no refetch — the lag only ever hit estimates saved before a price book existed.) **Pending:** prod promote. **Note:** the initial v5.30.4 "stale cached pricingSource" diagnosis was WRONG (console proved pricingSource was correctly `'native'`); the provider-flag session-restore refresh shipped in the same commit is kept only as harmless hardening, not the fix. Secondary follow-up **DONE (v5.37.2, 2026-06-13):** added a `finally` to `autoRefetchPriceBlocks` that calls `updateEditorToolbarState()` on every path (success / no-blocks / throw), so a future throw can't silently dead-end the toolbar. |
| Room-photo capture: Cancel + ghosting fix | **Done — Live in prod** (v5.30.1; verified 2026-06-11 — `cancelRoomPhotoCapture` in prod) | Room-photo capture now has a **Cancel** button and no longer ghosts the previously-captured photo into the new capture. |
| Marketing/trial two-URL split | **Done (v5.29.0) — Live in prod 2026-06-09** | `crewlogicai.com` = marketing (`marketing/index.html`, separate Cloudflare Pages project, output dir `marketing`); `app.crewlogicai.com` = app. Trial form fires env-aware signup → `crewlogic-signup` provisions a `trialing` native workspace; Phase 1b prefills company+name from `user_metadata`. Custom Resend SMTP wired into prod Supabase Auth. Full e2e verified in prod. See `docs/marketing-trial-onboarding-WIP.md`. **Remaining: Phase 2 onboarding wizard.** |
| Access gate: franchise `subscription_tier` overrides tenant status | **Fixed (code, v5.27.2 — 2026-06-07)** | `buildSessionFromSupabaseAuth` (index.html ~4185) sets access as `franchise.subscription_tier \|\| tenant.subscription_status \|\| 'trialing'`, so a franchise stuck on `'free'` **blocks** a user even when their tenant is `tester`/`trialing`/`active` → paywall. Hit tester **Thomas Baldwin (#54)** and **#31** (both `free` tier on `tester` tenants). Data fix applied (set those franchises' tier to `tester`). **Code follow-up:** the gate should grant access when EITHER the franchise tier OR the tenant status is an access value — a non-access franchise tier must not override an access-granting tenant. New tester/trial franchises default to `free`; **code fix shipped v5.27.2** — `buildSessionFromSupabaseAuth` (~4207) now grants access when EITHER the franchise tier OR the tenant status is an access value (a non-access tier no longer overrides), so this no longer recurs on setup. |
| AI photo analysis hangs / freezes (iPhone) | **Fixed (v5.26.3)** (2026-06-05) | `analyzeRoomPhotos` created a 45s `AbortController` but **never passed `controller.signal` to the fetch** — `callAnalyzeVoice` didn't accept it — so the abort was a no-op and a slow/stuck AI call hung the analyze UI indefinitely (owner: ~5 min frozen on iPhone, est `1780683980271`, had to refresh; photos uploaded fine, 0 charges produced). Fix: moved a REAL wired 60s timeout into `callAnalyzeVoice` (signal passed to fetch) so every analyze caller can actually abort → the existing catch blocks then show "timed out — try again" and re-enable. NB: if analysis *consistently* times out, that's a separate server-side `crewlogic-ai` perf issue (large photos / slow model) to investigate. |
| 🔴 Won/Lost/Reopen from list WIPES estimate charges (CRITICAL data loss) | **Fixed & Live in prod** (v5.26.1; verified 2026-06-11 — frontend PATCH-only [index.html:12967] + edge-fn 409 charge-wipe guard both in prod) | `updateEstimateStatus` (the list's Won/Lost/Reopen buttons, index.html:12565) re-saved the **whole payload** to the backend using the **list/local-cache** estimate object — which carries only lightweight fields, NOT `charges` — so every status click silently blanked that estimate's line items. Confirmed live on #90: **~15 Won/Lost estimates wiped** (charges gone; `total_price` + `vonigo_quote_id` preserved → line items recoverable from Vonigo). Reported via Turner/Halifax (`1780322856756`, Vonigo 678497); all 30 photos intact in storage. **Fix:** status change now does a targeted PostgREST PATCH of `status` + `updated_at` only (RLS `estimates_upd`, authenticated) — never touches the payload; worst case it 401s and the status simply doesn't change. **Recovery:** Vonigo quotes + per-device localStorage `cl_estimates_local`. |
| Estimate data-model stability: braces + normalization | **Braces live (v5.26.2); normalization Phases 1–3 done on dev — still DEV-ONLY in prod** (re-verified prod 2026-06-11: `estimate_charges`/`estimate_charge_photos` tables + dual-write ABSENT in prod; but the **charge-wipe 409 guard IS live** in the prod `crewlogic-estimate` fn). Migrations `0012` (table + franchise RLS) + `0013` (backfill, 38=38, 0 mismatches) applied to **dev only**. **Phase 3 (dual-write):** `crewlogic-estimate` save now best-effort mirrors charges into `estimate_charges` (delete-then-insert); deployed to **dev** + tested (save→2 rows, re-save→replaced, delete→cascade). Design: one row per charge, full object in `data jsonb` + promoted columns. **Remaining:** Phase 4 cut reads over (the big frontend rewiring) → Phase 5 retire blob. **Prod rollout (gated, coordinated):** apply 0012+0013 → deploy dual-write `crewlogic-estimate` → then Phase 4. | Stop-gaps on the JSON-blob design (real fix is normalization): (1) **server-side charge-wipe guard** in `crewlogic-estimate` save — rejects (409) any payload with no `charges` array when the stored row already has line items (protects ALL clients, incl. old builds still open in browsers); (2) **estimates list** no longer overwrites the local cache with an empty RLS result and shows "couldn't load — sign in" instead of a blank that reads as "deleted" (the lapsed-session scare). **Real fix:** move line items out of `payload.charges` into `estimate_charges` + `estimate_charge_photos` tables — phased plan (schema → backfill → dual-write → cut over reads) in `docs/plan-estimate-charges-normalization.md`. Needs prod deploy of `crewlogic-estimate` (edge fn) + index.html. |
| Vonigo deep-link from estimate | **Done (v5.27.0–v5.27.5, 2026-06-07)** | `Vonigo #<id>` is now a clickable link to `junkluggers.vonigo.com/Client/Quote/Quote_Main.aspx?quoteID=<id>` in all three spots: the estimate-editor pill (v5.27.0), the estimates-list card (v5.27.2), and the post-submit confirmation message (v5.27.5). |
| Show estimate IDs on the estimate editor view | **Done (v5.27.2, 2026-06-07)** | On the estimate detail/editor (the contact/Street-View/Notes view), surface the **Vonigo estimate #** + **CrewLogic estimate ID**, matching the estimates-list card (`✓ Vonigo #678497  ID:1780322856756`). Pairs with the deep-link above (the Vonigo # there should also be clickable) |
| Native "set up your price book" prompt | **Partly done (v5.25.0)** — Price Lookup screen now shows the friendly notice (no raw error) + single-list ZIP hint shipped; **still pending:** orange home Price Lookup tile (needs a `hasPriceBook` login signal) + general onboarding | Estimate editor now shows a clear "No price book set up yet → Set up Price Book" notice (with CTA) when native pricing can't load, instead of silently dimming the toolbar buttons (fixes the silent-failure that blocked estimate creation for Big Jakes in prod). Home Price Lookup badge is Vonigo-only, suppressed for native (v5.23.2). **Still (confirmed in "Space Junk" fresh-signup test, 2026-06-04):** (1) ~~Price Lookup screen showed the raw edge-fn error~~ **DONE (verified 2026-06-13):** friendly role-aware "No price book set up yet" notice + "Set up Price Book →" CTA now on `#priceLookupScreen` (`_setPriceLookupNoPriceBookNotice` via `doZipLookup`); (2) the **home Price Lookup tile should turn orange** for native owners with no price book yet (`updatePriceLookupCardState` ~5756 currently excludes native) — needs a `hasPriceBook` signal resolved at login; (3) general onboarding. Minor discoverability: with a single (default) list there's no ZIP UI — add a one-line hint that a single list is the catch-all for all ZIPs (ZIP routing appears once a 2nd list exists; working as designed) |
| Delete the default / only price list | **Done** (dev, v5.24.0) | Delete (✕) now shows for the default list too. `deletePriceList()`: blocks deleting the Default while other lists exist (prompts to make another Default first); allows deleting the last/only list (→ no price book, new-setup notice kicks in). Contextual confirm shows the ZIP-reassignment count + reassures that past estimates keep their snapshotted prices (estimates have no FK to price_lists). FK `ON DELETE CASCADE` handles blocks/items/zips |
| Native photo upload fails (storage RLS) | **Fixed** (dev+prod, 2026-06-03, migration `0011`) | Root cause confirmed: `estimate-photos` policies granted **anon only**, so a logged-in (magic-link) user's request — role `authenticated` — was denied on both `createSignedUrl` (cover *display* → 400, broken thumbnails) and `upload` (INSERT → "new row violates RLS"). Migration `0011_storage_authenticated_estimate_photos.sql` widens all three policies (SELECT/INSERT/DELETE) to `anon, authenticated`. Verified on dev with a real authenticated token (upload/sign/delete all 200), then applied to prod (policies confirmed `{anon,authenticated}`). Bucket-scoped only; per-franchise *path* scoping still deferred (Phase 3 §8). The session-less anon `storageClient` workaround (index.html:4323) is now redundant but harmless |
| Native pricing coverage model | **Built & Live in prod** (v5.26.0; verified 2026-06-11 — `toggleCatchAll`/CATCH-ALL in prod) — Explicit + optional catch-all (owner decision 2026-06-05; supersedes the 2026-06-03 "keep catch-all"). **Shipped (frontend only, `crewlogic-pricing` unchanged):** `createPriceList` no longer auto-defaults; **CATCH-ALL** badge + **"Covers all ZIPs"** toggle (`toggleCatchAll`); **Zips** button on every non-catch-all list; per-list item + ZIP counts; reframed warnings (empty catch-all / "nothing will price"); role-aware "this ZIP/town isn't priced — assign it / ask admin" messaging in estimate editor, Price Lookup ZIP **and** Town; `deletePriceList` reframed (catch-all now deletable). Reframes/closes #56.5 | Setup = create a list → **assign the towns/ZIPs you serve**; an **unassigned ZIP → "contact admin to set up"** (role-aware: owner gets "assign this ZIP"). A list can OPTIONALLY be toggled **"covers all other ZIPs"** (catch-all) — **off by default** (was: first list auto-became the catch-all). Repurposes the existing `is_default` flag as the opt-in catch-all (zero or one per franchise); `crewlogic-pricing` already does zip→list→catch-all→error, so the backend change is mostly the error copy/flag. Frontend work: (1) `createPriceList` no longer auto-sets `is_default`; (2) show **Zips** on every list; (3) replace "Make default" with a **"Covers all other ZIPs"** toggle + a **CATCH-ALL** badge; (4) reframe the empty-default warning (#56) → warn when a catch-all is empty OR nothing prices; (5) role-aware "ZIP not set up" messaging in estimate editor + Price Lookup; (6) onboarding (#57) = "assign your service towns/ZIPs". **Reframes #56.5** — under explicit, town autocomplete = assigned-towns-only is correct; town→any-ZIP resolution is only needed to *detect* "not assigned." **Migration:** existing `is_default` lists simply become the optional catch-all (no data change); existing catch-all franchises behave the same. |
| "Upon Completion" term in special terms | **Done (v5.25.0)** (2026-06-04) | Add an **Upon Completion** option to the special-terms "Balance due within" dropdown (`estSpecialTermsTermDays`, index.html:2159; currently — No term — / 30 / 60 / 90 days). Note: the field is stored & PDF-rendered as a **number of days** (`parseInt`, `(d>0)?d:''` ~11800; PDF cover page), so "Upon Completion" needs a non-numeric sentinel handled in `updateSpecialTermsDepositUI()` preview + the PDF cover-page renderer + save logic — not just a new `<option>`. From "Space Junk" test; additional-terms free-text already works |
| Sign-out doesn't fully sign out (security) | **Fixed (v5.25.0)** (2026-06-04) | Settings "Sign out" (`handleLogout`, index.html:5648) cleared only the local `SESSION_KEY` and left the Supabase Auth session (Google `signInWithOAuth` / magic-link) alive, so `initAuth` silently restored it on the next refresh — a sign-out that didn't sign out. Security gap on shared devices; affects prod junkluggers owners too. Fix: `handleLogout` now delegates to `signOut()` (full teardown incl. `supabaseClient.auth.signOut()`). Found in the "franchise 90" sign-out test. **Live in prod** (verified 2026-06-11 — `handleLogout`→`signOut`). |
| Default price list must be non-empty / catch-all clarity | **Partly done — part 5 (native Town→catch-all lookup) FIXED v5.27.7 (2026-06-08)**; empty-default warning + per-list item counts + single-list hint shipped (v5.25.0) | tpass2008 test (franchise `native-74f86026`): owner made 2 lists — "Tpass Price List 1" (`is_default=TRUE`, **0 items**, 0 ZIPs) and "Tpass Price List 2" (58 items, 15 ZIPs, **not default**). `crewlogic-pricing` correctly falls back to the default list for unmapped ZIPs (Danny Cronan `02726`, Rochester), but the default is **empty** → empty blocks → estimate editor shows "No price book set up yet" + dead toolbar, and Price Lookup fails; mapped ZIPs (Plymouth, on List 2) work. So the fallback works — the default list just has no prices. Fixes: (1) **warn when the default list has 0 items** (Price Book screen + at estimate time); (2) make "default = catch-all for all unmapped ZIPs" explicit + one-click to change which list is default; (3) the first-created (empty) list auto-became default while the populated one didn't — prefer defaulting the list that has items; (4) distinguish error copy: "no price book at all" vs "this ZIP isn't covered & your default list is empty"; (5) **Price Lookup for a default catch-all** (owner clarification 2026-06-04): native **Town** mode builds its autocomplete only from `price_list_zips` (`buildNativeTownIndex` ~6611) and **ZIP** mode needs the resolved default list to have items — so a default-only (no-ZIP) book can't look up anything by town and only prices mapped ZIPs. Desired: a native franchise on a default catch-all should look up **any town/ZIP within its Settings `serviceStates`/`officeState`**, pricing via the default list. The Vonigo lookup path already does this (state dropdown via `initPriceStateSelect` + Zippopotam `resolveTownToZip`) — reuse it for native default-catch-all. **FIX (v5.27.7, part 5):** `doTownLookup` native path now exact-matches assigned towns to their own list, and for any other town resolves town→ZIP via `resolveTownToZip` using the franchise's state(s) (derived from assigned ZIPs + `officeState`) → `doZipLookup` → catch-all applies. Verified on dev (franchise DEV1): "North Attleboro" (unassigned) → Zippopotam 02760 → "Everything" catch-all; assigned "Plymouth" → Waterford. Also dropped the loose contains/startsWith fuzzy match that mis-routed unassigned towns to an assigned neighbor (e.g. → Waterford's "Attleboro"). Remaining parts 1–4 (empty-default UX) still open. |
| New-estimate onboarding prompt (ZIPs + customers) | **Not started** (2026-06-04) | tpass2008 test: from main page → Estimates → New with no ZIPs/customers set up, owner expected a prompt to add ZIPs + import customers first. Add a first-run/empty-state prompt guiding native owners through price-book ZIPs + customer import before/at first estimate. Ties to the broader native onboarding item |
| Custom Google OAuth provisioning broken by RLS cutover | **Fixed & Live — prod edge fn deployed 2026-06-04, source now committed** (`crewlogic-oauth-callback`; verified 2026-06-11 — `SB_HEADERS` uses service role) | `crewlogictest@gmail.com` accepting an open estimator invite (into `native-74f86026`) via the invite screen's "Continue with Google" hit the app's "Something went wrong" screen. **Root cause (confirmed):** `crewlogic-oauth-callback` ran all PostgREST calls — profile lookup + **profile INSERT** + invite mark — with the **anon key** (`SB_HEADERS`). After the Phase-3 RLS cutover (migrations 0006–0010, 2026-06-03) `profiles` blocks anon inserts → `provisionFromInvite` returns `profile_create_failed`. Prod state proves it: crewlogictest got an `auth.users` row (via `signIntoSupabase`) but **no `profiles` row**; the invite stayed un-accepted (so retry is safe). Affects ALL new provisioning through the custom callback (Google invite-accept + self-serve Google signup); existing users with a profile and normal login (Supabase-native `signInWithOAuth`) are unaffected. **Fix (staged):** `SB_HEADERS` now uses `SUPABASE_SERVICE_ROLE_KEY` (auto-injected) so provisioning bypasses RLS — **needs a prod deploy of `crewlogic-oauth-callback` (gated).** The email-path invite (`crewlogic-accept-invite`) already uses the service role, so **"Email me a sign-in link" works now** as the workaround. My earlier "Google org-internal block" guess was wrong — Google let the account through; the callback's insert is what failed |
| Custom Google callback → no browser Supabase session → RLS blocks all data | **Fixed (v5.25.0)** — rerouted ALL Google logins (invite-accept, junkluggers-email, self-serve signup) to native `signInWithOAuth` so they get a real Supabase session; needs post-deploy testing of the Google round-trip (2026-06-04) | A user signed in via the **custom** Google callback (`triggerGoogleSignIn` → `crewlogic-oauth-callback`; used by invite-accept Google **and** junkluggers-email-routed Google via `loginV2GoogleForm`) gets a custom session in `SESSION_KEY` but **no real Supabase Auth session** — initAuth never calls `supabaseClient.auth.setSession()` (the callback passes `supabaseAccessToken` at index.ts:175 but the frontend ignores it, and no refresh token is sent). So `supabaseFetch` → `_supabaseUserToken()` returns null → every PostgREST call uses the **anon key**. Post-RLS-cutover: reads return **empty** and writes are **denied**. Confirmed live — estimator `crewlogictest` (profile correct: estimator, auth-linked, `native-74f86026`) saw **0 of 81 customers**, Town lookup couldn't find Plymouth despite its ZIP (02360) being mapped, and **Save Estimate silently failed** (exit re-prompts). The main login picker is unaffected (it uses Supabase-native `signInWithOAuth` → real session). **Fix:** route invite/junkluggers Google through `signInWithOAuth` (retire the custom callback for browser logins), or have the callback also return the refresh token and `setSession()` it in initAuth. **Workaround:** sign in via the **email magic-link** (real Supabase session → RLS works). NB: this is the deeper reason the estimate Save fails — separate from the v5.24.6 dirty-prompt fix |
| Estimator reaches owner-only Price Book setup (role gating) | **Fixed (v5.25.0)** — role-aware no-price-book notice (no CTA for estimators) + `showSettingsTab` blocks non-owners from owner-only tabs (2026-06-04) | `applyRoleRestrictions()` (index.html:5589) hides the Price Book/Franchise/Cost/Proposal Settings tabs from estimators, BUT the no-price-book notice's "Set up Price Book" CTA (line 1200) sets `_pendingSettingsTab='pricebook'` → on Settings load `showSettingsTab('pricebook')` (line 5932) renders that tab's content regardless of the hidden tab button → the estimator lands on the owner-only price-book setup screen. Two fixes: (1) make the no-price-book notice **role-aware** — estimators should NOT see a "Set up Price Book" CTA; show "Pricing isn't set up yet — ask your owner/admin" instead; (2) guard `_pendingSettingsTab`/`showSettingsTab` so an estimator can't deep-link into owner-only tabs. Found in crewlogictest estimator test. (The notice firing at all ties to the empty-default-list item: the price book exists but the **default** list has 0 items.) |
| Job plan: per-stop notes inline (not pooled at bottom) | **Done (v5.27.6, edge fn — 2026-06-07)** | Owner request: in the route brief, attach each stop's notes to **that stop** (time → city → description → its notes), in chronological order, instead of pooling all callouts in one `*NOTES:*` line at the bottom. Primary fix = prompt change in `crewlogic-job-plan/index.ts` (output-format spec ~lines 142–178; per-stop line is `"<TIME>, <CITY>: <description>"`, notes are a separate trailing `*NOTES:*` line — restructure so each stop carries its own notes). **Caveat:** check the frontend `renderJobPlan` parser doesn't depend on the bottom `*NOTES:*` line — it may need updating too, so not purely a prompt tweak. Desired shape: `8:00 AM, Marion: furniture & boat parts… — long carry (30' from driveway), heavy items` / `10:00 AM, Middleborough: upright piano, mattress… — customer absent, phone payment required, discretion on cost disclosure` / `1:00 PM, Mattapoisett: coolers, umbrellas… — repeat customer`, with `Load:` retained. **Frontend verified (2026-06-13):** `renderJobPlan` shows the plan text verbatim in a textarea, so the edge-fn's inline per-stop notes render correctly — no parser dependency on a bottom `*NOTES:*` line (optional callout highlighting was never requested). |
| v5.25.0 QA results (prod, 2026-06-05) | **Verified** | Owner (tpass2008) ran `docs/qa-test-plan-v5.25.0.md`. PASS: #59 estimator Google session (real session, 81 customers, save) **incl. fresh Google invite end-to-end (Test 8)**; #55 sign-out; #60 role-gating; #56 empty-default warning; #54 Upon Completion; v5.25.0 live. FAIL: Test 6 = the **unbuilt #56.5** Town catch-all lookup (not a regression). Open question logged: copy-price-list (Copy button already exists — clarify need) |
| Copy / seed a price list (clarify) | **Won't do — cancelled 2026-06-08** | Was really a workaround to get realistic data into dev for testing. Owner now has a standard **prod→dev data-refresh** step (post-production-refresh-back-to-test/dev), so per-list copy isn't needed. The in-app **Copy** button (`copyPriceList`, index.html:16670) still exists if a true list-duplication need arises later. |
| Estimate "Add Area" (volume charge) modal — reorder + show/override tier price | **Done — owner-tested in prod incl. Vonigo submit (2026-06-09)** | Shipped v5.28.0 (`235bd1e`) + v5.28.1 (`d0ad792`), live on prod. (1) **Volume/Qty moved to the TOP** of the Add Area modal (above Room Name), with uniform field heights. (2) **Editable per-tier volume price** in the volume footer breakdown (`#splitPricingBreakdown`, `updateTotals` ~11328): each tier renders Tier · ×Qty · editable $Price · Subtotal. (3) **Override** stored in `currentEstimate.volumeRateOverrides[fraction]` via `updateVolumeRate`/`resetVolumeRate` (~11238); `volumeRateFor(fraction)` (~11227) is the single override-aware rate source feeding `calcVolumePrice`/`calcVolumePriceSplit` AND the Vonigo submit line price (field 9287). Owner confirmed the Vonigo post carries the edited price (2026-06-09). Also fixed alongside: false "save or leave?" prompt on reopening an estimate (v5.28.2, `f3736e7`) — `saveCostAnalysisDraft` no longer dirties on programmatic restore / terminal estimates. |

---

_Convention: keep this file terse. One row per project. Link to the spec for detail. Stamp **Last
verified** whenever you confirm a row against reality._
