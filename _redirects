# Cloudflare Pages routing.
# Real static files (e.g. /start.html, assets) are served directly — static assets
# take priority over these rules. Everything else falls back to the single-page app
# (index.html) so deep links / unknown paths still load the app.
/*    /index.html   200
