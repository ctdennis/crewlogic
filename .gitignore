# macOS Finder metadata
.DS_Store
**/.DS_Store

# Claude Code personal/local settings (not shared)
.claude/settings.local.json

# Raw n8n workflow exports — may contain plaintext secrets; analyzed locally, never committed
**/n8n-workflows/*.json
docs/Route Optimization.json

# Local secret/key files (API keys pasted for one-off testing) — never commit
*-key.txt
*-token.txt
*token*.txt
motive-key.txt

# Local build/preview output — the app deploys root index.html directly (no build step); dist/ is not tracked
dist/
